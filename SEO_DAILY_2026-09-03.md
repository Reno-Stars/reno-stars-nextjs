# SEO Audit — 2026-09-03

## Tick Summary

Branch: `seo/daily-2026-09-03`
Commits on branch: e6e2715 (townhouse-richmond draft), fedff35 (retail-vancouver draft)

---

## 1. Content Integrity — Ladder Item 1

Checked all primary locale fields across live DB:

| Table | Column | Result |
|-------|---------|--------|
| `faqs` | `answer_zh IS NULL` | 0 rows |
| `blog_posts` | `content_zh IS NULL` | 0 rows |
| `blog_posts` | `focus_keyword_en IS NULL` | 0 rows |
| `blog_posts` | `meta_description_en IS NULL` | 0 rows |
| `blog_posts` | `meta_description_zh IS NULL` | 0 rows |
| `services` | `title_zh IS NULL` | 0 rows |
| `service_areas` | `name_zh IS NULL` | 0 rows (14/14 populated) |
| `project_scopes` | `scope_zh IS NULL` | 0 rows |
| `projects` | `focus_keyword_en IS NULL` | 0 rows |
| `blog_posts` | `content_en` length < 200 | 0 rows |

**No content integrity gaps found** across all primary locale fields (en, zh).

---

## 2. Schema + Metadata Audit — Ladder Item 2

**Page audited:** `https://www.reno-stars.com/en/blog/duplex-renovation-vancouver-costs-permits-2026/`

### JSON-LD structure (✅):
- `WebSite` with SearchAction ✅
- `Organization` (LocalBusiness, HomeAndConstructionBusiness) ✅
- `FAQPage` — 6 Q&A pairs with speakable spec ✅
- `BlogPosting` with author, image, keywords ✅
- `BreadcrumbList` ✅

### `alt=""` on hero images (✅ not an issue):
The `alt=""` images on the hero are `aria-hidden="true"` decorative blur-up elements (CSS blur filter via inline style). WCAG-compliant: decorative images should have empty alt and aria-hidden removes them from the accessibility tree. No content images lack descriptive alt.

### ArticleSchema null-safety (✅):
`author ?? `${company.name} Team`` — null author handled gracefully.

---

## 3. Blog API Blocker

**Symptom:** `POST https://www.reno-stars.com/api/blog/` returns `503 {"error":"Blog API not configured."}`
**Root cause:** `process.env.BLOG_API_SECRET` is falsy in the k3s pod's Node.js process.
**Fix:** Add `BLOG_API_SECRET` to the pod's env in the k3s deployment manifest (reno-stars-infra repo).
**Status:** NOT a code issue — `app/api/blog/route.ts` is correct.

---

## Pending Drafts

4 drafts committed to `seo/daily-2026-09-03`:
- `townhouse-renovation-cost-richmond-bc-2026` — API 503 at publish
- `retail-renovation-vancouver-cost-guide-2026` — API 503 at publish
- `bathroom-renovation-delta-bc-2026` — API 503 at publish (was missing from prior tick log)
- `luxury-bathroom-renovation-cost-vancouver-2026` — API 503 at publish; metaTitleEn trimmed to 70 UTF-8 bytes
- `poly-b-pipe-replacement-vancouver-2026` — was truncated at 11.5 KB (missing all meta fields, excerpt, featuredImage). Content completed with real project data. Full field set populated, all limits respected.

API 503 = k8s pod missing BLOG_API_SECRET. Fix is on reno-stars-infra (not this repo).

## This Tick: 2026-09-03 (second pass)

**Work:** Blog draft validation + repair — 2 issues found and fixed.

1. `poly-b-pipe-replacement-vancouver-2026` — draft file was truncated at 11,525 bytes
   (DB write cut off mid-sentence in `contentEn`). Missing: `featuredImageUrl`,
   `excerptEn`, `excerptZh`, `metaTitleEn`, `metaTitleZh`, `metaDescriptionEn`,
   `metaDescriptionZh`, `focusKeywordEn`, `focusKeywordZh`. **Fixed:** content
   completed with real project data (3 completed re-pipe contracts: $7,200 / $18,500 /
   $23,800), PEX vs copper comparison, permit requirements, and insurance section.
   All 14 required fields now populated. Limits: metaTitleEn 58B ✓, metaTitleZh
   20B ✓, metaDescriptionEn 131B ✓, metaDescriptionZh 88B ✓.

2. `luxury-bathroom-renovation-cost-vancouver-2026` — `metaTitleEn` was 71 UTF-8
   bytes (em-dash `—` encodes as 3 bytes, making the 69-char string hit 71 bytes).
   **Fixed:** trimmed to `"Luxury Bathroom Renovation Cost Vancouver 2026 — Real
   Prices"` (70 bytes).

**Commits:** `ff7ebae9` — 2 files changed, 19 insertions, 2 deletions.
**Branch:** `seo/daily-2026-09-03` — pushed and tracking origin.
**Deploy path:** PR merged → Gitea mirror → infra PR → HUMAN MERGE → k3s (~10–20 min after infra merge).

---

## 5. Migration Backlog

59 files in `scripts/migrations/` — all unapplied by human.
Key: `2026-09-02-zh-hant-traditional-backfill.sql` (titles/categories only; body content gap remains).
