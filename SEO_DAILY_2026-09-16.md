# SEO Tick 2026-09-16 — Branch seo/daily-2026-09-16

## Runtime Status
- `gh` NOT installed — cannot drain PRs via `gh pr list` (confirmed: `gh pr list` → "command not found"). `git ls-remote` confirmed no open PR refs on the remote.
- DB API: Live. Queries via `POST https://www.reno-stars.com/api/db/query` with `agent_ro` credential.
- Site: Live at https://www.reno-stars.com (HTTP 200).

## Checks Run This Tick

### Content Integrity (Ladder Item 1)
DB queries on all core tables — **0 rows** needing work:
- `blog_posts`: excerpt_zh IS NULL/empty → 0 rows
- `blog_posts`: meta_title_zh > 70 chars → 0 rows
- `blog_posts`: meta_description_zh > 155 chars → 0 rows
- `services`: description_zh IS NULL/empty → 0 rows
- `service_areas`: description_zh IS NULL/empty → 0 rows
- `project_scopes`: scope_zh IS NULL/empty → 0 rows
- `projects`: excerpt_zh IS NULL/empty → 0 rows
- `project_sites`: excerpt_zh IS NULL/empty → 0 rows
- `project_sites`: description_zh IS NULL/empty → 0 rows
- `faqs`: answer_zh IS NULL/empty → 0 rows

### Schema / Technical (Ladder Item 2)
- Homepage (/) JSON-LD: WebSite + Organization/LocalBusiness/HomeAndConstructionBusiness + FAQPage — all present and valid.
- Blog post (`/en/blog/pre-sale-renovation-white-rock-bc-2026/`) JSON-LD: WebSite + Organization + BreadcrumbList + FAQPage + BlogPosting — all present. BlogPosting includes `headline`, `description`, `datePublished`, `author`, `publisher`, `mainEntityOfPage`, `speakable`, `image`, `inLanguage`, `keywords`.
- ArticleSchema.tsx: `dateModified` suppression logic confirmed — only emitted when caller provides a trustworthy value (fabricating it is correctly avoided).
- ArticleSchema.tsx: `articleSection` field supported but omitted when not provided (correct — honest omission over generic guess).
- Homepage hreflang: All 14 locales present (en, zh, zh-Hant, ja, ko, es, pa, tl, fa, vi, ru, ar, hi, fr) + x-default — correct.
- Homepage `og:locale:alternate`: All 14 ogLocale values present — correct (all 14 locales are indexable).
- robots.txt: AI bot block list covers all major AI search engines (GPTBot, ChatGPT-User, OAI-SearchBot, PerplexityBot, ClaudeBot, Claude-Web, Claude-SearchBot, Google-Extended, Applebot-Extended, Bingbot, CCBot, anthropic-ai, cohere-ai, Diffbot, omgili). No indexable page disallowments. Sitemap linked.
- `<html lang="en" dir="ltr">`: Correct on homepage.
- llms.txt: Live at `/llms.txt` (HTTP 200, text/plain).
- `buildAlternateLocales()`: Correctly uses `ogLocaleMap` for all 14 locales — not filtered by `nativeSupport`. Verified in source.

### Coverage / Structure (Ladder Item 3)
- Sitemap: 7 sitemap index entries (pages, services, service-areas, project-hubs, projects×2, blog) — all dated 2026-09-15T03:21:50.530Z.
- Service detail (`/en/bathroom-renovation/`): ContactPage schema confirmed present.
- Homepage: FAQPage schema confirmed present.
- Blog listing (`/en/blog/`): BlogPosting schema confirmed present on article pages.

### Pending Migrations Backlog
- `scripts/migrations/pending/`: **13 files** — AT BACKLOG CAP (capped per ladder rules).
  - 8 old blog drafts (.json + .sql pairs from 2026-08-24/28)
  - 1 blog-coverage-gap-audit.sql
  - 4 published-ready blog drafts (.json) from prior sessions
  - No new migration files written this tick.

## Branch Status
- `seo/daily-2026-09-16` already has 3 commits from prior session today:
  1. `e98287d6` — seo: fix Reno Stars → 聚星裝修 in renovation-deposit-bc-guide draft
  2. `af6786a3` — fix(blog-drafts): replace Reno Stars → 聚星裝修 in all Chinese fields across 21 drafts
  3. `1e630b7a` — seo: fix brand name (Reno Stars→聚星装修) and field limits in 10 blog drafts
- 17 blog-draft JSON files already committed to branch.
- `blog-drafts/bathroom-renovation-delta-bc-2026.json` field-length validated manually (all within varchar limits, no "Reno Stars" in zh fields).

## Deployment
PR merged → Gitea mirror (<=10 min) → infra PR requires human merge → cronjob applies within 5 min. This tick did not produce any code changes requiring deploy.

## Action Items for Human
1. Review and publish any of the 17 committed blog drafts: `pnpm blog:publish -f blog-drafts/<slug>.json --publish`
2. Apply pending migrations in `scripts/migrations/pending/` — 13 files awaiting human `psql` run.
3. If a deploy is needed, merge the infra PR on Reno-Stars/reno-stars-infra after this branch merges to main.
