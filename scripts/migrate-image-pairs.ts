/**
 * OBSOLETE: This migration script was used to migrate data from the old
 * projectImages and siteImages tables to the new imagePairs tables.
 *
 * The old tables (project_images, site_images) have been removed from the schema.
 * This script is kept for historical reference only.
 *
 * DO NOT RUN THIS SCRIPT - the old tables no longer exist.
 */

console.log('This migration script is obsolete.');
console.log('The old projectImages and siteImages tables have been removed.');
console.log('Data now uses projectImagePairs and siteImagePairs tables.');
process.exit(0);
