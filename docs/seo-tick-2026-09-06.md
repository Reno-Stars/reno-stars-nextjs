# SEO Tick Notes — 2026-09-06

## robots.txt — Code Fixed, Deploy Blocked
**Status**: Documented. No new action this tick.

## CONTENT INTEGRITY CHECK — 2026-09-06 PASS
All columns clean. Pending migrations cover the 2 remaining blog_posts with NULL zh fields.

## LADDER 2 CHECK — north-vancouver-home-renovation-guide-2026
Live page: correct meta, hreflangs, og tags, og:locale:alternate (all 14 locales), JSON-LD (Organization/LocalBusiness schema), content renders.
BlogPosting schema: not present — Organization schema only (no action available in this tick).
Blog sitemap: post not yet in blog-0.xml (likely generated on schedule; hreflang alternates present on-page).
Internal project links: post links to /en/projects/ pages, no real project slugs — noted for future post writing.

## LADDER 1 MIGRATION COMMITTED
scripts/migrations/2026-09-06-blog-meta-description-zh-outdoor-test.sql — meta_description_zh + focus_keyword_zh for outdoor-test-mtonly (247e6fde). NOT APPLIED — needs human run.


**File**: `app/robots.ts` (on `origin/main` ✅)

**Current state**: `app/robots.ts` has explicit `allow: '/'` rules for:
- GPTBot, ChatGPT-User, OAI-SearchBot, PerplexityBot
- ClaudeBot, Claude-Web, Claude-SearchBot
- Google-Extended, Applebot-Extended, Bingbot

**Live site serving**: Stale robots.txt blocking all the above crawlers.

**Root cause**: k8s pod (`ns/reno-stars`) is running a pre-fix Docker image.
The code fix was merged to `origin/main` but the infra PR on
`Reno-Stars/reno-stars-infra` has NOT been merged yet (human gate).

**Fix**: Human must merge the infra PR that updates the Reno Stars pod image.
No code change needed — the correct `app/robots.ts` is already on main.

**Impact**: GEO/AI Overview visibility directly harmed. Claude Bot,
GPTBot, PerplexityBot etc. cannot crawl the site, preventing inclusion
in AI-powered search results (ChatGPT, Copilot, Perplexity, Gemini).
