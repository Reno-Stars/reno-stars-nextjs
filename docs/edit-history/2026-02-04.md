# 2026-02-04

## Session 1 — SEO Fixes, Client Feedback, Code Review, Hydration Fix, Projects Migration to DB

### Summary

Fixed critical SEO double-slash URL bug, addressed 8 client feedback items (section reordering, category card redesign, contact page layout, form styling, modal glow, mobile layout), ran code review and fixed all warnings, resolved hydration mismatch from nested `<html>` tags in not-found pages, migrated projects from static data to database queries, merged `middleware.ts` into `proxy.ts` for Next.js 16 compatibility, and added admin dashboard (projects, blog, testimonials, contacts, company, services).

### SEO Fixes

#### Double-Slash URL Bug (`lib/utils.ts`)

- **Root cause:** `NEXT_PUBLIC_BASE_URL` env var had a trailing slash, causing every canonical, hreflang, sitemap, and robots.txt URL to contain `//` (e.g., `reno-stars.com//en/`).
- **Fix:** `getBaseUrl()` now strips trailing slashes: `url.replace(/\/+$/, '')`.
- Affected: `app/sitemap.ts`, `app/robots.ts`, all `generateMetadata` functions via `buildAlternates()`.

#### Metadata Length Fixes (`messages/en.json`, `messages/zh.json`)

- Shortened homepage title (77→44 chars), description (175→136 chars)
- Shortened services page title (81→43 chars), description (172→127 chars)
- Corresponding Chinese translations updated

#### Favicon (`app/icon.png`, `app/apple-icon.png`)

- Added 32×32 favicon and 180×180 Apple touch icon using Next.js App Router file convention

#### Security (`next.config.ts`)

- Added `poweredByHeader: false` to disable `X-Powered-By: Next.js` header

### Navigation

#### Added `/services` to Navigation (`components/Navbar.tsx`, `components/Footer.tsx`)

- Inserted `/services` link between Home and Projects in both Navbar `navLinks` and Footer `quickLinks` arrays

### Client Feedback (8 Items)

#### 1. Homepage Section Reorder (`components/pages/HomePage.tsx`)

- Reordered sections below hero: **Testimonials → Gallery → Services** → Stats → About → Trust Badges → Blog → Showroom → Contact
- Previously: Services → Gallery → Testimonials

#### 2. Category Cards Redesign (`components/pages/ProjectsPage.tsx`)

- Changed from square cards with bottom text to 3:4 aspect ratio portrait cards
- Darker overlay (0.45 default, 0.55 active) with centered large text
- Added hover scale effect via `group-hover/cat:scale-105`
- Active indicator: gold underline bar

#### 3. Contact Page Layout (`components/pages/ContactPage.tsx`)

- Widened container from `max-w-6xl` to `max-w-7xl`
- Increased body text from `text-sm` to `text-base`
- Form shown first on mobile (`order-1 lg:order-2`)

#### 4. Contact Form Styling (`components/ContactForm.tsx`)

- Larger inputs: `py-3 rounded-xl text-base` (was py-2.5 rounded-lg text-sm)
- Focus rings replaced: `--tw-ring-color` → standard `outline-0 focus:outline-2 focus:outline-offset-1` with `outlineColor: GOLD`
- Textarea: 5 rows (was 4), larger submit button

#### 5. Project Modal Glow (`components/ProjectModal.tsx`)

- Simplified shadow from double shadow with `neu(8)` to single `0 25px 50px rgba(27,54,93,0.25)`

#### 6. Mobile Contact Form Priority

- Contact form shows first on mobile via CSS `order-1 lg:order-2`

### Code Review Fixes

- **ContactForm.tsx** — Replaced fragile `--tw-ring-color` Tailwind internal with standard CSS `outlineColor` property
- **ContactPage.tsx** — Wrapped `onSuccess` callback in `useCallback`, `contactInfo` array in `useMemo`
- **ProjectsPage.tsx** — Memoized `neu(4)` shadow value as `neuShadow4`

### Hydration Mismatch Fix (Layout Restructure)

#### Problem

Root `app/not-found.tsx` rendered its own `<html>/<body>` tags. When a 404 occurred within the `[locale]` layout (which also rendered `<html>/<body>`), nested `<html>` tags caused React hydration mismatch: server rendered `lang="zh"` but client expected `lang="en"`.

#### Solution — Single `<html>/<body>` in root layout only

- **`app/layout.tsx`** — Now provides `<html lang={locale}><body>` using `getLocale()` from next-intl/server (previously just returned `children`)
- **`app/[locale]/layout.tsx`** — Removed `<html>`, `<head>`, `<body>` tags. Renders `NextIntlClientProvider` + `LocalBusinessSchema` + `Navbar` + `Footer` directly
- **`app/not-found.tsx`** — Removed `<html>/<body>` tags. Just renders 404 content div
- **`app/[locale]/not-found.tsx`** — New file. Locale-aware 404 page using `getTranslations('common')`, renders inside locale layout (no `<html>/<body>`)

### Middleware → Proxy Migration

#### Problem

Next.js 16 deprecated `middleware.ts` in favor of `proxy.ts`. Both files existed, causing a startup crash: "Both middleware file and proxy file are detected."

#### Solution

- **Merged `middleware.ts` into `proxy.ts`** — Combined admin auth (session cookie check, redirect to `/admin/login`) with i18n routing (next-intl middleware) and security headers (CSP, X-Frame-Options, etc.)
- **Deleted `middleware.ts`**
- All responses now get security headers via `addSecurityHeaders()` helper
- Uses `routing` from `i18n/config` (consistent with middleware.ts) instead of individual locale params

### Projects Migration to Database

#### New Query Functions (`lib/db/queries.ts`)

Added 5 new project-related query functions:

| Function | Returns | Notes |
|----------|---------|-------|
| `getProjectsFromDb()` | `Project[]` | All published projects, cached with `cache()` |
| `getProjectBySlugFromDb(slug)` | `Project \| null` | Single project lookup, cached |
| `getAllProjectsAdmin()` | Raw DB rows with images/scopes | Admin use, not cached |
| `getProjectSlugsFromDb()` | `string[]` | For sitemap generation |
| `getAllServicesAdmin()` | Raw service rows | Admin use |
| `getAllTestimonialsAdmin()` | Raw testimonial rows | Admin use |
| `getAllBlogPostsAdmin()` | Raw blog post rows | Admin use |
| `getAllContactsAdmin()` | Raw contact rows | Admin use |

Internal helpers: `mapDbProjectToProject()` (maps DB rows → `Project` type), `fetchProjectRelations()` (batch-loads images + scopes).

#### Updated Page Routes

- **`app/[locale]/projects/page.tsx`** — Fetches projects from DB via `getProjectsFromDb()`, passes as prop to `ProjectsPage`
- **`app/[locale]/projects/[slug]/page.tsx`** — Uses `getProjectsFromDb()` and `getProjectBySlugFromDb()` instead of static `getProjectBySlug()`. Removed `generateStaticParams` (now dynamic)
- **`app/sitemap.ts`** — Changed from sync to async. Uses `getProjectSlugsFromDb()` for project URLs instead of static array

#### Updated Components

- **`ProjectsPage`** — Now receives `projects: Project[]` as prop. Derives filter options (locations, space types, budget ranges) from props via `useMemo` instead of calling static helper functions
- **`ProjectDetailPage`** — Receives `project` and `allProjects` as props instead of looking up by slug internally
- **`ProjectCategoryPage`** — Receives `projects: Project[]` as prop instead of importing static data

### Admin Dashboard (New)

New admin panel at `/admin/` with:

- **Auth** (`lib/admin/auth.ts`) — Session cookie-based authentication with 24h TTL
- **Layout** (`app/admin/layout.tsx`) — Admin shell with sidebar navigation
- **Dashboard pages** (`app/admin/(dashboard)/`) — CRUD for projects, blog, testimonials, contacts, company, services
- **Server actions** (`app/actions/admin/`) — Mutation functions for projects, blog, testimonials, contacts, company, services
- **Components** (`components/admin/`) — DataTable, ProjectForm, BilingualInput, BilingualTextarea, ImageUrlInput, ConfirmDialog, Sidebar, TopBar, StatusBadge, ToastProvider, FormField
- **Seed script** (`scripts/seed-projects.ts`) — Imports static project data into DB

### Session 2 — Migrate Remaining Static Data to DB + Blog Crawler

## Session 2 — Migrate Remaining Static Data to DB, Blog Crawler, Code Review Fixes

### Summary

Migrated all remaining static data (service areas, blog posts, gallery items, trust badges, showroom info) from `lib/data/` to database queries. Built a WordPress blog crawler that seeds 22 articles with bilingual content via WP REST API (EN) and HTML page crawling (ZH). Fixed code review issues: XSS protection via `sanitize-html`, gallery category capitalization, gallery seed idempotency via unique index, and homepage blog limit of 5.

### Static Data → Database Migration

#### New Query Functions (`lib/db/queries.ts`)

Added 7 new cached query functions:

| Function | Returns | Notes |
|----------|---------|-------|
| `getServiceAreasFromDb()` | `ServiceArea[]` | Active areas, ordered by `displayOrder` |
| `getBlogPostsFromDb()` | `BlogPost[]` | Published posts, ordered by `publishedAt desc` |
| `getBlogPostBySlugFromDb(slug)` | `BlogPost \| null` | Single post lookup |
| `getBlogPostSlugsFromDb()` | `string[]` | For sitemap (uncached) |
| `getGalleryItemsFromDb()` | `GalleryItem[]` | Published items, `getAssetUrl()` applied to images |
| `getTrustBadgesFromDb()` | `{ en: string; zh: string }[]` | Active badges, ordered by `displayOrder` |
| `getShowroomFromDb()` | `Showroom` | Singleton row |

Gallery categories are capitalized in the query layer (`'whole-house'` → `'Whole House'`).

#### Updated Server Routes (fetching from DB)

- `app/[locale]/layout.tsx` — `getServiceAreasFromDb()` replaces static import, passes `areas` to `LocalBusinessSchema`
- `app/[locale]/page.tsx` — Fetches gallery, trust badges, blog posts (limited to 5), showroom, areas from DB
- `app/[locale]/blog/page.tsx` — Fetches blog posts from DB
- `app/[locale]/blog/[slug]/page.tsx` — Fetches post by slug, `generateStaticParams` async
- `app/[locale]/design/page.tsx` — Fetches gallery from DB
- `app/[locale]/contact/page.tsx` — Fetches areas, extracts locale-specific names
- `app/[locale]/areas/[city]/page.tsx` — Fetches areas from DB, finds by slug
- `app/[locale]/services/[service-slug]/[city]/page.tsx` — Fetches areas from DB
- `app/sitemap.ts` — Uses `getBlogPostSlugsFromDb()` and `getServiceAreasFromDb()`

#### Updated Client Components (props instead of static imports)

- `HomePage` — New props: `gallery`, `trustBadges`, `blogPosts`, `showroom`, `areas`; inline localization via `useMemo`
- `BlogPage` — New prop: `blogPosts`
- `BlogPostPage` — Changed from `postSlug: string` to `post: BlogPost` prop
- `DesignPage` — New prop: `gallery`
- `AreaPage` — Changed from `citySlug: string` to `area: ServiceArea` prop
- `ContactPage` — New prop: `areaNames: string[]`
- `ServiceLocationPage` — Now receives `area: ServiceArea` prop
- `LocalBusinessSchema` — Receives `areas` as prop

#### Static Data Cleanup

- `lib/data/index.ts` — Removed all static arrays (showroom, gallery, blogPosts, trustBadges) and helper functions. Kept: `video`, `images`, `getLocalizedBlogPost()`, type re-exports, project/service/area re-exports
- `lib/data/areas.ts` — Reduced to single `getLocalizedArea()` function (removed static `serviceAreas` array and all lookup helpers)

### Blog Crawler (`scripts/seed-blog.ts`)

New script that crawls the old WordPress site for blog content:

- **English**: Fetched via WP REST API (`/wp-json/wp/v2/renovation_article`) — structured JSON
- **Chinese**: Crawled from `/zh/renovation_article/{slug}/` HTML pages (WPML doesn't expose ZH via REST API)
- **Title extraction**: Tries `og:title`, `<title>`, `<h1>` tags; prefers whichever contains Chinese characters
- **Content cleaning**: Strips scripts, styles, shortcodes, inline styles, data attributes
- **Idempotent**: Uses `onConflictDoUpdate` to refresh content on re-run
- **Result**: 22 articles seeded, 14/22 with Chinese titles, all with distinct ZH body content where translations exist
- **Command**: `pnpm db:seed:blog`

### Code Review Fixes

1. **XSS protection** (`components/pages/BlogPostPage.tsx`) — Added `sanitize-html` to sanitize blog content before `dangerouslySetInnerHTML`
2. **Gallery category display** (`lib/db/queries.ts`) — Capitalizes DB enum values (`'kitchen'` → `'Kitchen'`, `'whole-house'` → `'Whole House'`)
3. **Gallery seed idempotency** (`lib/db/schema.ts` + `lib/db/seed.ts`) — Added `uniqueIndex` on `gallery_items.image_url`; replaced existence check with `onConflictDoNothing({ target: galleryItems.imageUrl })`
4. **Blog seed content** (`lib/db/seed.ts`) — Removed hardcoded blog posts (now handled by crawler script)
5. **Homepage blog limit** (`app/[locale]/page.tsx`) — `blogPosts.slice(0, 5)` limits homepage to 5 most recent posts

### Dependencies

- Added `sanitize-html` (runtime) + `@types/sanitize-html` (dev)

### Files Changed

| File | Action |
|------|--------|
| `app/layout.tsx` | Modified (added `<html>/<body>` with locale detection) |
| `app/[locale]/layout.tsx` | Modified (removed `<html>/<body>/<head>`) |
| `app/not-found.tsx` | Modified (removed `<html>/<body>`) |
| `app/[locale]/not-found.tsx` | **New** (locale-aware 404) |
| `proxy.ts` | Modified (merged admin auth + security headers) |
| `middleware.ts` | **Deleted** |
| `lib/utils.ts` | Modified (trailing slash fix in `getBaseUrl()`) |
| `lib/db/queries.ts` | Modified (added project + admin query functions) |
| `app/sitemap.ts` | Modified (async, uses DB for project slugs) |
| `app/[locale]/projects/page.tsx` | Modified (DB-fetched projects) |
| `app/[locale]/projects/[slug]/page.tsx` | Modified (DB-fetched projects, removed generateStaticParams) |
| `components/pages/HomePage.tsx` | Modified (section reorder) |
| `components/pages/ProjectsPage.tsx` | Modified (receives projects as prop, memoized shadow) |
| `components/pages/ProjectDetailPage.tsx` | Modified (receives project as prop) |
| `components/pages/ProjectCategoryPage.tsx` | Modified (receives projects as prop) |
| `components/pages/ContactPage.tsx` | Modified (useMemo/useCallback, wider layout) |
| `components/ContactForm.tsx` | Modified (outline focus, larger inputs) |
| `components/ProjectModal.tsx` | Modified (simplified shadow) |
| `components/Navbar.tsx` | Modified (added /services link) |
| `components/Footer.tsx` | Modified (added /services link) |
| `messages/en.json` | Modified (shortened metadata) |
| `messages/zh.json` | Modified (shortened metadata) |
| `next.config.ts` | Modified (poweredByHeader: false) |
| `app/icon.png` | **New** (32×32 favicon) |
| `app/apple-icon.png` | **New** (180×180 touch icon) |
| `i18n/config.ts` | Modified (exported routing) |
| `package.json` | Modified (dependencies) |
| `app/admin/` | **New** (admin dashboard) |
| `app/actions/admin/` | **New** (admin server actions) |
| `components/admin/` | **New** (admin UI components) |
| `lib/admin/` | **New** (admin auth) |
| `scripts/seed-projects.ts` | **New** (project data seeder) |
