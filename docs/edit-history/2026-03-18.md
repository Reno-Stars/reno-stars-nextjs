# 2026-03-18

## Session 1: Batch Upload — Unsupported Format Tracking, ZIP Context Hints, Better Fallback Metadata

Three features added to the batch upload pipeline, followed by code review fixes.

### New Features (pre-review changes)

#### 1. Unsupported image format tracking
- **Files:** `lib/batch/types.ts`, `lib/batch/zip-parser.ts`, `lib/batch/batch-processor.ts`
- Added `UNSUPPORTED_IMAGE_EXTENSIONS` set (heic, heif, tiff, tif, bmp, raw) and `isUnsupportedImageFile()` helper in zip-parser
- Both `parseZip()` and `parseZipStandalone()` now collect skipped filenames into a `skippedFiles` array
- Added `skippedFiles: string[]` field to both `ParsedZipStructure` and `ParsedStandaloneStructure` interfaces
- `pushSkippedFilesWarning()` surfaces skipped files as warnings in the job error list

#### 2. ZIP filename as PO number hint (standalone mode)
- **Files:** `lib/batch/batch-processor.ts`
- Standalone mode extracts `zipBaseName` from the uploaded filename (e.g., "2828-van.zip" → "2828-van")
- For single-project ZIPs, the base name is passed to AI metadata generation as a `Reference/PO number` context hint
- Also passed through `saveProject()` as `zipBaseName` for fallback PO number usage

#### 3. Better fallback metadata via `ensureActionSuffix()`
- **Files:** `lib/batch/batch-processor.ts`
- New `ensureActionSuffix(label, zh?)` function ensures labels include action words (Renovation/装修/翻新/改造)
- Refactored `buildFallbackSeo()` to accept pre-computed `fullEn`/`fullZh` labels instead of raw name + type
- `fallbackSiteData()` and `fallbackProjectData()` now use `ensureActionSuffix()` for consistent labels
- Removed generic `FALLBACK_ALT` constant — site image pair alt text now uses contextual titles (e.g., "Kitchen Renovation - Before 1")
- Fallback site descriptions no longer include folder name (often internal codes)

### Code Review Fixes

#### 4. Removed unused `_folderName` parameter from `fallbackProjectData()`
- **Files:** `lib/batch/batch-processor.ts`
- Parameter was made unused by the refactor but kept with underscore prefix — removed entirely along with the argument at the call site

#### 5. Removed redundant `singleProjectZipBase` alias
- **Files:** `lib/batch/batch-processor.ts`
- Was just an alias of `aiZipHint` with no transformation — now uses `aiZipHint` directly

#### 6. Truncated long skipped files warnings
- **Files:** `lib/batch/batch-processor.ts`
- `pushSkippedFilesWarning()` now lists max 10 filenames, with "and N more" suffix for overflow
- Prevents unbounded warning messages when ZIPs contain many HEIC/TIFF files

#### 7. Added word boundaries to `ensureActionSuffix` EN regex
- **Files:** `lib/batch/batch-processor.ts`
- Changed `/renovation|refacing|remodel/i` to `/\b(renovation|refacing|remodel)\b/i` for more precise matching

#### 8. Added unit tests for new utility functions
- **Files:** `tests/unit/batch/batch-processor.test.ts`
- 6 tests for `ensureActionSuffix` (EN append, EN skip, case-insensitive, word boundary, ZH append, ZH skip)
- 5 tests for `pushSkippedFilesWarning` (empty, basic, path stripping, truncation at 10, no "more" at exactly 10)
- Both functions exported for testability

## Session 2: AI Output Review — Hybrid Validation for Project Descriptions

Added a two-layer validation system for AI-generated project descriptions to catch invalid scopes, bad service types, and slug-title mismatches.

### New Features

#### 1. Programmatic validation (`validateProjectDescription`)
- **Files:** `lib/ai/content-optimizer.ts`
- Pure function that runs immediately after Zod parsing in `optimizeProjectDescription()`
- **Scope enforcement:** Filters `selectedScopes` to only entries existing in the provided scope list; falls back to all scopes if every AI-selected scope was invalid
- **ServiceType enforcement:** Sets `serviceType` to `null` if not in the allowed list
- **Slug-title consistency:** Regenerates slug from title via `formatSlug()` when slug and title share fewer than `MIN_SLUG_TITLE_OVERLAP` (2) significant words (stop words excluded)
- Returns `{ corrected, corrections }` — corrections array used for logging
- Deep-copies `selectedScopes` array to avoid mutating input

#### 2. Lightweight AI review (`reviewProjectDescription`)
- **Files:** `lib/ai/content-optimizer.ts`
- Second, cheaper AI call using `gpt-4o-mini` with `temperature: 0.1` and 512 max tokens
- Sends only the raw notes + key generated fields (title, serviceType, slug, scopes) to a focused reviewer prompt
- Returns only fields that need correction as a partial object; returns `null` if everything is correct
- Non-fatal — wrapped in try/catch, failures just log a warning and skip the review
- `ReviewCorrectionsSchema` uses strict Zod (no `.passthrough()`) to drop unknown fields

#### 3. Integration into `optimizeProjectDescription()`
- **Files:** `lib/ai/content-optimizer.ts`
- After Zod parse: (1) programmatic validation always runs, (2) AI review runs and applies corrections on top
- Review corrections are re-validated (serviceType checked against allowed list, slug sanitized through `formatSlug()`, scopes re-validated)
- Return type extended to `ProjectDescription & { corrections?: string[] }`
- All corrections logged via `console.log('[AI Validation]', ...)`

#### 4. Batch processor logging
- **Files:** `lib/batch/batch-processor.ts`
- `generateProjectMetadata()` logs corrections from the validation/review step
- Existing scope filter at save time (lines 1098-1103) remains as a safety net

#### 5. `maxTokensReview` config
- **Files:** `lib/ai/openai.ts`
- Added `maxTokensReview: 512` to `AI_CONFIG`

### Code Review Fixes

#### 6. ServiceType re-validation after AI review
- AI review could return a serviceType not in the allowed list — now re-validated after applying review corrections

#### 7. Slug sanitization after AI review
- Reviewer-provided slug is run through `formatSlug()` to ensure valid URL format

#### 8. Removed `.passthrough()` from ReviewCorrectionsSchema
- Unknown fields from AI response are now stripped by Zod

#### 9. Deep copy for selectedScopes in validateProjectDescription
- Changed `{ ...result }` to `{ ...result, selectedScopes: [...result.selectedScopes] }` to prevent mutation

#### 10. Set-based slug word comparison
- Slug words now use `Set.has()` instead of `Array.includes()`

#### 11. Named constant for slug overlap threshold
- Extracted magic number `2` to `MIN_SLUG_TITLE_OVERLAP`

#### 12. Single-pass scope partition
- Replaced two `.filter()` calls with one loop

#### 13. JSDoc placement fix
- Moved `type ReviewCorrections` above JSDoc block so doc comment associates with the function

### Tests

#### 14. Unit tests for `validateProjectDescription`
- **Files:** `tests/unit/lib/ai/content-optimizer.test.ts`
- 14 tests across 5 describe blocks: serviceType enforcement (4), scope enforcement (5), slug-title consistency (3), input immutability (1), combined corrections (1)

## Session 3: Fix Batch Upload AI — Zod Schema Resilience & Error Logging

Batch upload was silently producing fallback "Kitchen Renovation" data for ZIPs with valid `notes.txt`. Root cause: GPT-4o-mini sometimes omits SEO fields (`metaTitleEn`, `metaDescriptionEn`, etc.) from the JSON response, causing `ProjectDescriptionSchema.parse()` to throw a `ZodError`. The catch block in `generateProjectMetadata()` silently swallowed the error and returned `null`, triggering kitchen-specific fallback data.

### Bug Fix

#### 1. Added `.default('')` to optional Zod schema fields
- **Files:** `lib/ai/content-optimizer.ts`
- `ProjectDescriptionSchema`: 16 fields changed from `z.string()` to `z.string().default('')` — `locationCity`, `poNumber`, `budgetRange`, `durationEn/Zh`, `challengeEn/Zh`, `solutionEn/Zh`, `badgeEn/Zh`, `excerptEn/Zh`, `metaTitleEn/Zh`, `metaDescriptionEn/Zh`, `focusKeywordEn/Zh`, `seoKeywordsEn/Zh`
- `SiteDescriptionSchema`: same pattern for 14 fields
- Core fields (`slug`, `titleEn/Zh`, `descriptionEn/Zh`, `detectedLanguage`) remain required — these are essential and their absence indicates a real AI failure
- Prevents ZodError when the AI omits non-critical fields

#### 2. Error logging in AI metadata generators
- **Files:** `lib/batch/batch-processor.ts`
- `generateSiteMetadata()`: catch block now logs `console.error` with folder name and error message (was completely silent)
- `generateProjectMetadata()`: catch block now logs `console.error` with folder name and error message (was completely silent)
- Corrections logging changed from `console.log` to `console.warn` (matches eslint allowed list)

## Session 4: Dynamic Service Types — Remove Hardcoded Values

Service type dropdown, AI prompts, and batch upload pipeline were using hardcoded service type lists instead of loading from the database. Fixed all three layers.

### Admin Form

#### 1. Added empty option to service type dropdown
- **Files:** `components/admin/ProjectForm.tsx`, `messages/admin/en.ts`, `messages/admin/zh.ts`
- Service type `<select>` now has `<option value="">Select service type...</option>` as first option
- Initial state defaults to `''` instead of `services[0]?.slug` (both `useState` and reset `useEffect`)
- Server action already handles empty → `null` conversion at save time

### AI Pipeline

#### 2. Admin AI action now passes DB service types and scopes
- **Files:** `app/actions/admin/optimize-content.ts`
- `optimizeProjectDescriptionAction()` loads `getServiceTypeMap()` from DB and passes `availableServiceTypes` to `optimizeProjectDescription()`
- All unique scopes from `SERVICE_SCOPES` deduplicated into module-level `ALL_SCOPES` constant (computed once via IIFE)
- Previously, the admin action called `optimizeProjectDescription(rawNotes)` with no type/scope constraints — AI guessed freely

#### 3. Review prompt uses dynamic service types
- **Files:** `lib/ai/content-optimizer.ts`
- `REVIEW_PROMPT` constant replaced with `buildReviewPrompt(availableServiceTypes?)` function
- Hardcoded `"Valid values: bathroom, kitchen, basement, cabinet, commercial"` replaced with dynamic list from caller
- `reviewProjectDescription()` accepts `availableServiceTypes` parameter and threads it through
- `optimizeProjectDescription()` passes `availableServiceTypes` to the review call

### Batch Upload Pipeline

#### 4. `detectServiceType()` returns `null` instead of hardcoded default
- **Files:** `lib/batch/zip-parser.ts`, `lib/batch/types.ts`
- Was: `DEFAULT_SERVICE_TYPE = 'kitchen'` — any unrecognized folder name defaulted to kitchen
- Now: returns `null` when no alias matches, letting AI detect the actual type from notes
- Accepts optional `validTypes: Set<string>` to validate alias targets against DB service types
- `ParsedProject.serviceType` type changed from `string` to `string | null`

#### 5. `parseZip()` and `parseZipStandalone()` accept `validServiceTypes`
- **Files:** `lib/batch/zip-parser.ts`, `lib/batch/batch-processor.ts`
- Both parsers accept optional `validServiceTypes: Set<string>` parameter
- Batch processor builds `validServiceTypes` from `getServiceTypeMap()` and passes it to parsers
- Ensures `detectServiceType()` alias targets are validated against actual DB service types

#### 6. Batch processor handles `null` service type throughout
- **Files:** `lib/batch/batch-processor.ts`
- `generateProjectMetadata()` accepts `string | null` — builds generic "Renovation project" prompt when type is null
- `fallbackProjectData()` accepts `string | null` — uses generic "Renovation" / "装修" labels when type is null
- Scope lookup at save time uses AI-resolved `serviceType` with null fallback

### CI Fix

#### 7. Seed projects exits gracefully when WordPress unreachable
- **Files:** `scripts/seed-projects.ts`
- `process.exit(1)` → `process.exit(0)` for both "no projects discovered" and "no projects crawled" cases
- Unrelated to service type changes but fixed CI build failure in same session

### Tests

#### 8. Added `getServiceTypeMap` mock to optimize-content tests
- **Files:** `tests/unit/actions/optimize-content.test.ts`
- Mock returns `{ kitchen, bathroom }` map to satisfy the new DB dependency

## Session 5: Crawlable Language Switcher & Areas Navigation

Fixed two SEO issues where the language switcher and "Areas" nav trigger were `<button>` elements invisible to crawlers.

### Changes

#### 1. Language switcher: `<button>` → `<Link>`
- **Files:** `components/Navbar.tsx`
- Desktop and mobile language toggles changed from `<button onClick={switchLocale}>` to `<Link href={pathname} locale={otherLocale}>`
- Renders a real `<a href="/zh/current-path">` that crawlers can follow to discover alternate-language pages
- Removed dead code: `switchLocale` callback, `useRouter` import

#### 2. Areas nav: split into crawlable link + dropdown toggle
- **Files:** `components/Navbar.tsx`
- Desktop and mobile "Areas" triggers changed from a single `<button>` to a `<Link href="/areas">` (text) + `<button>` (chevron icon)
- Crawlers can now discover the `/areas/` listing page from the nav
- Dropdown still opens/closes via the chevron button for users
- Added `top-full` to dropdown for correct positioning with the new `flex` container

#### 3. Translation keys for chevron aria-label
- **Files:** `messages/en.json`, `messages/zh.json`
- Added `nav.toggleAreas` key ("Toggle areas menu" / "展开区域菜单")
- Chevron buttons use this instead of generic `nav.areas` to differentiate from the adjacent link for screen readers

#### 4. Touch target sizing
- Chevron buttons use `p-2` (desktop) and `p-2 -mr-2` (mobile) for ~40px hit area
- Replaced `!pr-0` Tailwind override with explicit `px-3 py-2 pr-1` classes on the Areas link

## Session 6: Crawlable Project Cards

Made project cards render as crawlable `<a>` links instead of `<button>` elements, improving internal linking and PageRank flow to project detail pages.

### Changes

#### 1. Added `href` prop to `ProjectCard`
- **Files:** `components/ProjectCard.tsx`
- New optional `href` prop; when provided, renders as `<Link href={...}>` from `@/navigation` (next-intl, auto-adds locale prefix)
- When both `href` and `onClick` are provided, click handler calls `e.preventDefault()` then the callback — crawlers see a real `<a href="/en/projects/slug/">`, JS users still get the modal
- Added `block` class to ensure consistent `display: block` layout across all render paths (`<Link>`, `<button>`, `<article>`)
- Fallback behavior preserved: `onClick` only → `<button>`, neither → `<article>`

#### 2. Passed `href` at all usage sites
- **Files:** `components/pages/ProjectsPage.tsx`, `components/pages/ProjectCategoryPage.tsx`, `components/RelatedProjectsSection.tsx`
- All three pass `href={`/projects/${project.slug}`}` alongside existing `onClick={handleCardClick}`

## Session 7: Enrich Service Area Pages

Transformed thin, templated area pages into substantive local content pages with unique intro copy, area-specific FAQs, DB-sourced projects, custom highlights, and per-area SEO metadata. Previously all 14 area pages shared the same hardcoded benefits, generic descriptions, and static project data — making them look like doorway pages.

### Schema Changes (21 files, +517/-90 lines)

#### 1. Extended `service_areas` table with 8 new columns
- **Files:** `lib/db/schema.ts`
- `contentEn`/`contentZh` (text, nullable) — rich unique intro copy per area
- `highlightsEn`/`highlightsZh` (text, nullable) — newline-separated custom benefits
- `metaTitleEn`/`metaTitleZh` (varchar(120), nullable) — custom SEO meta title
- `metaDescriptionEn`/`metaDescriptionZh` (varchar(320), nullable) — custom SEO meta description

#### 2. Added `serviceAreaId` FK to `faqs` table
- **Files:** `lib/db/schema.ts`
- `serviceAreaId` UUID FK with `onDelete: 'set null'` — null = global FAQ, non-null = area-specific
- New index: `faqs_service_area_id_idx`

### Types & Data Layer

#### 3. Extended `ServiceArea` and `LocalizedArea` types
- **Files:** `lib/types.ts`, `lib/data/areas.ts`
- Added `id`, `content`, `highlights`, `metaTitle`, `metaDescription` to both interfaces
- `getLocalizedArea()` maps all new fields

#### 4. New and updated query functions
- **Files:** `lib/db/queries.ts`
- `getServiceAreasFromDb()` — now returns `id` and all new fields; `parseNewlineList()` helper converts highlights text to string arrays
- `getFaqsByAreaFromDb(areaId)` — new cached query for area-specific FAQs
- `getProjectsByAreaFromDb(cityName)` — new cached query, case-insensitive `locationCity` match, returns up to 6 projects with relations
- `mapFaqRows()` — extracted shared helper to deduplicate FAQ row mapping between `getFaqsFromDb()` and `getFaqsByAreaFromDb()`
- `getFaqsFromDb()` — now filters to global FAQs only (`WHERE service_area_id IS NULL`)

### Area Page Enrichment

#### 5. Server component data fetching
- **Files:** `app/[locale]/areas/[city]/page.tsx`
- `generateMetadata()` uses custom `metaTitle`/`metaDescription` when available, with i18n-generated fallback
- Single `Promise.all` for all 5 dependent queries after area resolution
- `FAQSchema` structured data rendered when area has FAQs

#### 6. Client component enrichment
- **Files:** `components/pages/AreaPage.tsx`
- New "Unique Content" section — renders `area.content[locale]` as paragraphs (split by double-newline)
- Area-specific FAQs via reused `FaqSection` component
- Custom highlights with fallback to hardcoded `areaBenefits` i18n keys
- DB-sourced project cards replacing static data (via `getProjectsByAreaFromDb`)
- Removed imports of deprecated `getAllProjectsLocalized` static data

#### 7. Made `FaqSection` subtitle optional
- **Files:** `components/home/FaqSection.tsx`
- `subtitle` prop changed from required to optional; conditionally renders `<p>` only when present

### Admin Enhancements

#### 8. Service area form: new fields
- **Files:** `app/admin/(dashboard)/service-areas/ServiceAreaForm.tsx`, `app/admin/(dashboard)/service-areas/[id]/page.tsx`
- `BilingualTextarea` for content (rows=8) and highlights (rows=5, "One benefit per line" tooltip)
- `BilingualInput` for meta title (maxLength=120) and meta description (maxLength=320)
- SEO Settings section header with helper text

#### 9. Service area actions: handle new fields + meta validation
- **Files:** `app/actions/admin/service-areas.ts`
- `createServiceArea` and `updateServiceArea` handle all 8 new fields
- Server-side `validateTextLengths` for meta title (120) and meta description (320)
- `deleteServiceArea` checks both contact and FAQ references before allowing deletion

#### 10. FAQ form: area scope dropdown
- **Files:** `app/admin/(dashboard)/faqs/FaqForm.tsx`, `app/admin/(dashboard)/faqs/FaqsListClient.tsx`, `app/admin/(dashboard)/faqs/[id]/page.tsx`, `app/admin/(dashboard)/faqs/new/page.tsx`, `app/admin/(dashboard)/faqs/page.tsx`
- New `serviceAreaId` dropdown (Global / specific area) on FAQ create/edit forms
- FAQ list shows area column with locale-aware area name display
- `parseServiceAreaId()` helper validates UUID in form data
- `AreaOption` interface in `lib/admin/constants.ts`

#### 11. i18n: new translation keys
- **Files:** `messages/admin/en.ts`, `messages/admin/zh.ts`, `messages/en.json`, `messages/zh.json`
- Admin: `faqs.serviceArea`, `faqs.globalFaq`, `serviceAreas.contentLabel/Help`, `serviceAreas.highlightsLabel/Help`, `serviceAreas.metaTitleLabel`, `serviceAreas.metaDescriptionLabel`, `serviceAreas.seoSettings/seoHelp`
- Public: `areas.faqTitle`, `areas.aboutArea`

### Production Data Seeding

#### 12. Seed area content script
- **Files:** `scripts/seed-area-content.ts` (new)
- Populated all 14 areas with unique bilingual content, highlights, meta titles, and meta descriptions
- Inserted 8 area-specific FAQs (Vancouver 2, Richmond 2, Burnaby 1, Surrey 1, West Vancouver 1, North Vancouver 1)

#### 13. SEO fix script
- **Files:** `scripts/fix-area-seo.ts` (new)
- Fixed all 14 meta titles to ≤60 chars with "Reno Stars" brand
- Fixed all 14 meta descriptions to 140-160 chars range
- Removed em dashes, AI-tell words ("seamless", "stunning", "vibrant"), templated patterns from 8 areas' content
- Added 13 new FAQs for 8 areas that had zero (Coquitlam, New Westminster, Delta, Langley, Port Moody, Maple Ridge, White Rock, Port Coquitlam)

### Code Review Fixes (4 rounds)
- Extracted `mapFaqRows()` to deduplicate FAQ row mapping
- Made `FaqSection` subtitle optional to prevent empty DOM element
- Merged two sequential `Promise.all` into one in area page
- Added server-side meta field length validation
- Removed unused `Faq` and `Project` type imports
