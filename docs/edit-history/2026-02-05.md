# Edit History — 2026-02-05

## Summary

Added client-side locale switching to admin panel, allowing administrators to toggle between English and Chinese content display. Performed code review and fixed identified issues. Fixed 12 pre-existing ESLint unused variable warnings across the codebase.

---

## Admin Locale Support

### New Files

- **`components/admin/AdminLocaleProvider.tsx`** — React context for admin locale state
  - Provides `locale` and `setLocale` via context
  - Persists preference in localStorage (`admin_locale` key)
  - Defaults to English on first visit
  - `useAdminLocale()` hook for consuming components

### Modified Files

- **`components/admin/TopBar.tsx`** — Added locale switcher buttons
  - EN/ZH toggle buttons with gold highlight for active locale
  - Uses `useAdminLocale()` hook

- **`app/admin/(dashboard)/layout.tsx`** — Wrapped with `AdminLocaleProvider`

- **List Clients Updated for Locale-Aware Display:**
  - `app/admin/(dashboard)/blog/BlogListClient.tsx` — titleEn/titleZh
  - `app/admin/(dashboard)/projects/ProjectsListClient.tsx` — titleEn/titleZh
  - `app/admin/(dashboard)/faqs/FaqsListClient.tsx` — questionEn/questionZh
  - `app/admin/(dashboard)/testimonials/TestimonialsListClient.tsx` — textEn/textZh
  - `app/admin/(dashboard)/gallery/GalleryListClient.tsx` — titleEn/titleZh
  - `app/admin/(dashboard)/service-areas/ServiceAreasListClient.tsx` — nameEn/nameZh
  - `app/admin/(dashboard)/trust-badges/TrustBadgesListClient.tsx` — badgeEn/badgeZh

### Pattern Used

All list clients follow this pattern:
\`\`\`tsx
const { locale } = useAdminLocale();

const columns = useMemo(() => {
  const getField = (row) => locale === 'zh' ? row.fieldZh : row.fieldEn;
  return [
    { key: locale === 'zh' ? 'fieldZh' : 'fieldEn', header: locale === 'zh' ? 'Field (ZH)' : 'Field (EN)' },
    // ...
  ];
}, [locale, ...]);
\`\`\`

---

## Code Review Fixes

### FaqsListClient.tsx

- **Migrated from native `confirm()` to `ConfirmDialog`** — Matches pattern used in other list clients
- Added `deleteId` state and `ConfirmDialog` component
- Added `pendingId` state for toggle button loading

### GalleryListClient.tsx

- **Added `'titleZh'` to searchKeys** — Now searches both English and Chinese titles

### TestimonialsListClient.tsx

- **Added `'textEn', 'textZh'` to searchKeys** — Now searches testimonial content in both languages

---

## Documentation Updates

- **CLAUDE.md** — Added admin locale switching documentation under Component Conventions
- **docs/architecture.md** — Added admin locale switching section, updated admin components list, fixed homepage section count (12 files)
- **docs/edit-history/2026-02-05.md** — Created session edit history

---

## ESLint Warning Fixes

Fixed 12 pre-existing ESLint unused variable warnings across the codebase.

### Removed Unused Imports

| File | Removed |
|------|---------|
| `app/[locale]/loading.tsx` | `NAVY` |
| `app/[locale]/projects/[slug]/page.tsx` | `getLocalizedService` |
| `components/pages/ProjectCategoryPage.tsx` | `SURFACE_ALT`, `TEXT` |
| `components/pages/ProjectsPage.tsx` | `CARD`, `TEXT_MUTED` |

### Prefixed Unused Parameters

| File | Change |
|------|--------|
| `components/pages/ContactPage.tsx` | `locale` → `_locale` |
| `components/pages/ServiceLocationPage.tsx` | `citySlug` → `_citySlug` |
| `components/pages/ServicesPage.tsx` | `locale` → `_locale` |
| `components/pages/ThankYouPage.tsx` | `locale` → `_locale` |

### Removed Unused Variables

| File | Change |
|------|--------|
| `app/admin/(dashboard)/contacts/ContactsListClient.tsx` | Removed unused `isPending` from component-level useTransition |

### Refactored Code

- **`app/actions/admin/about.ts`** — Restructured to build `textFields` object directly instead of destructuring to exclude `updatedAt`, avoiding the unused variable warning
