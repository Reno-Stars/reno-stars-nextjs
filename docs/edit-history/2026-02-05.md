# Edit History — 2026-02-05

## Summary

Added client-side locale switching to admin panel, allowing administrators to toggle between English and Chinese content display. Performed code review and fixed identified issues. Fixed 12 pre-existing ESLint unused variable warnings across the codebase.

---

## Admin Locale Support

### New Files

- **`components/admin/AdminLocaleProvider.tsx`** — React context for admin locale state
  - Provides `locale` and `setLocale` via context
  - Persists preference in localStorage (`admin_locale` key)
  - Defaults to English on first visit
  - `useAdminLocale()` hook for consuming components

### Modified Files

- **`components/admin/TopBar.tsx`** — Added locale switcher buttons
  - EN/ZH toggle buttons with gold highlight for active locale
  - Uses `useAdminLocale()` hook

- **`app/admin/(dashboard)/layout.tsx`** — Wrapped with `AdminLocaleProvider`

- **List Clients Updated for Locale-Aware Display:**
  - `app/admin/(dashboard)/blog/BlogListClient.tsx` — titleEn/titleZh
  - `app/admin/(dashboard)/projects/ProjectsListClient.tsx` — titleEn/titleZh
  - `app/admin/(dashboard)/faqs/FaqsListClient.tsx` — questionEn/questionZh
  - `app/admin/(dashboard)/testimonials/TestimonialsListClient.tsx` — textEn/textZh (later deleted in Google Reviews migration)
  - `app/admin/(dashboard)/gallery/GalleryListClient.tsx` — titleEn/titleZh
  - `app/admin/(dashboard)/service-areas/ServiceAreasListClient.tsx` — nameEn/nameZh
  - `app/admin/(dashboard)/trust-badges/TrustBadgesListClient.tsx` — badgeEn/badgeZh

### Pattern Used

All list clients follow this pattern:
\`\`\`tsx
const { locale } = useAdminLocale();

const columns = useMemo(() => {
  const getField = (row) => locale === 'zh' ? row.fieldZh : row.fieldEn;
  return [
    { key: locale === 'zh' ? 'fieldZh' : 'fieldEn', header: locale === 'zh' ? 'Field (ZH)' : 'Field (EN)' },
    // ...
  ];
}, [locale, ...]);
\`\`\`

---

## Code Review Fixes

### FaqsListClient.tsx

- **Migrated from native `confirm()` to `ConfirmDialog`** — Matches pattern used in other list clients
- Added `deleteId` state and `ConfirmDialog` component
- Added `pendingId` state for toggle button loading

### GalleryListClient.tsx

- **Added `'titleZh'` to searchKeys** — Now searches both English and Chinese titles

---

## Google Reviews Migration (replaced DB testimonials)

Replaced manually-managed database testimonials with live Google Reviews fetched via the Google Places API (New, v1). Removed all admin CRUD for testimonials.

### New Files

- **`lib/google-reviews.ts`** — Fetches reviews from Google Places API
  - Two parallel requests (MOST_RELEVANT + NEWEST sort) for maximum coverage
  - Deduplicates by author name, filters to 5-star reviews only
  - 24h server-side cache via `next: { revalidate: 86400 }`
  - Env vars: `GOOGLE_PLACES_API_KEY`, `GOOGLE_PLACE_ID`
  - Graceful fallback: returns empty result if env vars missing or API fails

- **`components/home/GoogleAvatar.tsx`** — Client component for Google profile photos
  - `'use client'` to support `onError` fallback (server components can't use event handlers)
  - Falls back to navy circle with initial letter on load failure

### Modified Files

- **`lib/types.ts`** — Replaced `Testimonial` interface with `GoogleReview` and `GooglePlaceRating`
- **`lib/data/index.ts`** — Removed `Testimonial`/`LocalizedTestimonial` exports, added `GoogleReview`/`GooglePlaceRating`
- **`components/home/TestimonialsSection.tsx`** — Full rewrite
  - Horizontal scrolling marquee with CSS `@keyframes` animation (no client JS)
  - Cards rendered twice for seamless infinite loop, pause on hover
  - Google "G" SVG icon linking to reviewer's Google profile
  - `line-clamp-5` for review text, `Intl.RelativeTimeFormat` for locale-aware dates
- **`components/structured-data/ReviewSchema.tsx`** — Uses `GooglePlaceRating` type, fixed `bestRating` to `5`
- **`components/pages/HomePage.tsx`** — Changed `testimonials` prop to `googleReviews: GooglePlaceRating`
- **`app/[locale]/page.tsx`** — Uses `getGoogleReviews()` instead of `getTestimonialsFromDb()`
- **`next.config.ts`** — Added `lh3.googleusercontent.com` to `images.remotePatterns`
- **`proxy.ts`** — Added `https://lh3.googleusercontent.com` to CSP `img-src` directive
- **`lib/db/queries.ts`** — Removed `getTestimonialsFromDb()` and `getAllTestimonialsAdmin()`
- **`lib/db/seed.ts`** — Removed testimonial seeding block
- **`lib/db/schema.ts`** — Added TODO/deprecated comment on testimonials table (kept to avoid migration)
- **`.env.example`** — Added `GOOGLE_PLACES_API_KEY` and `GOOGLE_PLACE_ID` documentation
- **`components/admin/Sidebar.tsx`** — Removed testimonials nav link
- **`app/admin/(dashboard)/page.tsx`** + **`DashboardClient.tsx`** — Removed testimonials stat card
- **`messages/admin/en.ts`** + **`zh.ts`** — Removed testimonials translation keys

### Deleted Files

- `app/admin/(dashboard)/testimonials/` (entire directory: page.tsx, TestimonialsListClient.tsx, TestimonialForm.tsx, new/page.tsx, [id]/page.tsx)
- `app/actions/admin/testimonials.ts`

---

## Documentation Updates

- **CLAUDE.md** — Added admin locale switching documentation under Component Conventions; updated for Google Reviews migration (removed testimonials references, added Google Reviews env vars, updated admin content count from 13 to 12)
- **docs/architecture.md** — Added admin locale switching section, updated admin components list, fixed homepage section count (12 files); updated for Google Reviews migration (query layer table, admin queries, data architecture)
- **docs/database.md** — Updated for Google Reviews migration (removed testimonials from seeding, query layer, admin queries)
- **docs/seo.md** — Updated ReviewSchema description for Google Reviews
- **docs/README.md** — Updated project highlights for Google Reviews migration
- **docs/edit-history/2026-02-05.md** — Created session edit history, added Google Reviews migration section

---

## ESLint Warning Fixes

Fixed 12 pre-existing ESLint unused variable warnings across the codebase.

### Removed Unused Imports

| File | Removed |
|------|---------|
| `app/[locale]/loading.tsx` | `NAVY` |
| `app/[locale]/projects/[slug]/page.tsx` | `getLocalizedService` |
| `components/pages/ProjectCategoryPage.tsx` | `SURFACE_ALT`, `TEXT` |
| `components/pages/ProjectsPage.tsx` | `CARD`, `TEXT_MUTED` |

### Prefixed Unused Parameters

| File | Change |
|------|--------|
| `components/pages/ContactPage.tsx` | `locale` → `_locale` |
| `components/pages/ServiceLocationPage.tsx` | `citySlug` → `_citySlug` |
| `components/pages/ServicesPage.tsx` | `locale` → `_locale` |
| `components/pages/ThankYouPage.tsx` | `locale` → `_locale` |

### Removed Unused Variables

| File | Change |
|------|--------|
| `app/admin/(dashboard)/contacts/ContactsListClient.tsx` | Removed unused `isPending` from component-level useTransition |

### Refactored Code

- **`app/actions/admin/about.ts`** — Restructured to build `textFields` object directly instead of destructuring to exclude `updatedAt`, avoiding the unused variable warning
