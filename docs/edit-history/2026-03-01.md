# Edit History: 2026-03-01

## Summary

Three sets of changes: (1) Sprint 1 foundations — public error boundary, DB indexes, sitemap site slugs, fullscreen modal accessibility, contact page structured data; (2) Notes.txt content guide for batch upload; (3) Example ZIP download button.

---

## Sprint 1: Foundations (commit `f1c7c31`)

Fixed 6 foundational issues across the codebase.

### Item 0: Fix failing Sidebar test (CI blocker)

- Added missing `partners: 'Partners'` to `nav` and `batchUpload: { navLabel: 'Batch Upload' }` to the `useAdminTranslations` mock in `Sidebar.test.tsx`

### Item 1: Public error boundary (`error.tsx`)

- Created `app/[locale]/error.tsx` — `'use client'` error boundary with neumorphic card, `role="alert"`, Try Again button (GOLD), and Back to Home link (NAVY outline)
- Uses plain `<a href="/">` instead of `<Link>` since router may be broken in error boundaries
- Added `tryAgain` translation key to `messages/en.json` and `messages/zh.json`

### Item 2: Database indexes for `isPublished`

- Added 3 indexes to `lib/db/schema.ts`:
  - `project_sites_published_show_idx` on `(isPublished, showAsProject)`
  - `projects_is_published_idx` on `(isPublished)`
  - `blog_posts_is_published_idx` on `(isPublished)`
- Pushed to production via direct SQL (Drizzle `db:push` had unrelated schema drift on batch_upload_jobs jsonb columns — resolved by casting text→jsonb)

### Item 3: Sitemap site slugs

- Added `getSiteSlugsFromDb()` query in `lib/db/queries.ts` — fetches published sites with `showAsProject=true`
- Updated `app/sitemap.ts` to include site slug entries at `/projects/{site-slug}/` (priority 0.6, monthly)
- Added dedup guard: site slugs that collide with project slugs are skipped

### Item 4: Fullscreen modal accessibility

- Created `hooks/useFullscreenModal.ts` — manages scroll lock, focus trap, keyboard navigation (Escape/Arrow/Tab), and return focus to trigger element
- Applied to `ProjectDetailPage.tsx` and `SiteDetailPage.tsx` — overlay now has `role="dialog"`, `aria-modal="true"`, `aria-label`

### Item 5: Contact page structured data

- Created `components/structured-data/ContactPageSchema.tsx` — `@type: ContactPage` with `HomeAndConstructionBusiness` + `ContactPoint`
- Extracted shared `parseAddress()` to `components/structured-data/parse-address.ts` (used by both `LocalBusinessSchema` and `ContactPageSchema`)
- Wired into `app/[locale]/contact/page.tsx`

### Code review fixes

- Deduplicated `parseAddress` function (extracted to shared module)
- Replaced inline arrow button handlers with callback references in fullscreen overlays
- Fixed hardcoded `/en/` locale URL in ContactPageSchema (now uses locale prop)
- Added `projectSlugSet` filter to prevent sitemap slug collisions between projects and sites

### Files Modified

| File | Changes |
|------|---------|
| `tests/unit/components/admin/Sidebar.test.tsx` | Added missing mock keys |
| `messages/en.json` | Added `common.tryAgain` |
| `messages/zh.json` | Added `common.tryAgain` |
| `lib/db/schema.ts` | Added 3 isPublished indexes |
| `lib/db/queries.ts` | Added `getSiteSlugsFromDb()` |
| `app/sitemap.ts` | Added site slug entries with dedup |
| `components/pages/ProjectDetailPage.tsx` | Applied `useFullscreenModal` hook |
| `components/pages/SiteDetailPage.tsx` | Applied `useFullscreenModal` hook |
| `components/structured-data/LocalBusinessSchema.tsx` | Extracted `parseAddress` to shared module |
| `components/structured-data/index.ts` | Added `ContactPageSchema` export |
| `app/[locale]/contact/page.tsx` | Rendered `ContactPageSchema` |

### New Files

| File | Purpose |
|------|---------|
| `app/[locale]/error.tsx` | Public error boundary with neumorphic design |
| `hooks/useFullscreenModal.ts` | Fullscreen modal a11y hook (scroll lock, focus trap, keyboard nav) |
| `components/structured-data/ContactPageSchema.tsx` | Contact page JSON-LD structured data |
| `components/structured-data/parse-address.ts` | Shared address parsing utility |

---

## Notes.txt Content Guide (commit `538ed75`)

Added documentation in the batch upload help section explaining what to include in `notes.txt` files.

### Changes

- Split `folderStructureHelp` translation into structure-only text
- Added `notesHelpTitle` and `notesHelpBody` translation keys
- Added example notes.txt block in the help UI with fields: Location, Budget, Timeline, Style, Materials, Challenges, Features

### Files Modified

| File | Changes |
|------|---------|
| `app/admin/(dashboard)/batch-upload/BatchUploadClient.tsx` | Added notes.txt documentation section |
| `messages/admin/en.ts` | Added `notesHelpTitle`, `notesHelpBody`, updated `folderStructureHelp` |
| `messages/admin/zh.ts` | Added `notesHelpTitle`, `notesHelpBody`, updated `folderStructureHelp` |

---

## Example ZIP Download Button (commit `263b961`)

Added a "Download Example ZIP" button to the admin batch upload page.

### Changes

- Copied `test-data/richmond-whole-house-test.zip` to `public/example-batch-upload.zip` (12KB static asset)
- Added gold-colored download link with download icon next to the "Expected Folder Structure" help button
- Link uses `<a download>` for native browser download behavior, with hover underline effect
- Added bilingual translation key `batchUpload.downloadExample` ("Download Example ZIP" / "下载示例ZIP")

### Files Modified

| File | Changes |
|------|---------|
| `app/admin/(dashboard)/batch-upload/BatchUploadClient.tsx` | Wrapped help button and new download link in a flex row |
| `messages/admin/en.ts` | Added `batchUpload.downloadExample` key |
| `messages/admin/zh.ts` | Added `batchUpload.downloadExample` key |

### New Files

| File | Purpose |
|------|---------|
| `public/example-batch-upload.zip` | Example ZIP with correct folder structure for batch upload reference |

---

### Verification

- `pnpm test:run` — 310/310 tests pass
- `pnpm typecheck` — 0 errors
- `pnpm lint` — clean
- DB indexes pushed to production via SQL
