# 2026-03-27 — Editable Service Slug, Client-Side Video Compression, Video Upload Support

## Summary

**Session 1:** Made the service slug field editable in the admin panel's edit mode. Previously, the slug was read-only when editing a service (only settable during creation). Now follows the same pattern as blog posts and projects, with `ensureUniqueSlug()` collision handling. Added unit test for slug collision and improved E2E test reliability (replaced `waitForTimeout` with proper toast assertions).

**Session 2:** Added client-side video compression with ffmpeg.wasm. Videos uploaded through `VideoUrlInput` are automatically compressed (CRF 18, near-lossless H.264) before upload to S3, reducing bandwidth for phone/camera recordings. Includes progress bar UI, smart skip logic (files > 500 MB or already-optimized), unmount guards, and singleton race condition protection. Three rounds of code review with all issues fixed. Added 19 unit tests for pure helpers and 4 E2E tests for the compression UI.

**Session 3:** Added full-stack video upload support to projects, sites, and before/after image pairs. Extended the database schema with `heroVideoUrl` on `projects` and `project_sites` tables, and `beforeVideoUrl`/`afterVideoUrl` on both image pair tables. Updated CHECK constraints, query layer, type system, localization helpers, admin forms, server actions, and front-end display components.

## Editable Service Slug

### ServiceForm slug field unified
- `app/admin/(dashboard)/services/ServiceForm.tsx`: Removed the conditional branch that rendered a read-only `<input>` in edit mode. Replaced with a single `<input name="slug">` using `defaultValue` and `fieldStyle`, which respects the existing `<fieldset disabled={!editing}>` toggle. Same pattern as all other fields in the form.

### updateService action — slug handling added
- `app/actions/admin/services.ts`: `updateService()` now reads `slug` from form data, lowercases and trims it. Validates with `isValidSlug()`. Fetches the current service's slug via `db.select()` for the `excludeSlug` parameter. Queries conflicting slugs with `like(services.slug, ...)`. Calls `ensureUniqueSlug(slug, conflictingSlugs, currentSlug)` — same pattern as `updateBlogPost()` (`blog.ts:128-132`). Includes `slug` in the `.set()` update payload.

### Unit tests
- `tests/unit/actions/services.test.ts`: Added `updateService` test verifying `ensureUniqueSlug` is called with current slug for collision handling. Mocks `db.select()` with call-count-based responses (first call returns current slug, second returns conflicting slugs).

### E2E tests
- `tests/e2e/admin-service-slug.spec.ts`: Replaced `waitForTimeout(2000)` in `submitAndWaitForSuccess` with proper `getByRole('alert').getByText('Service saved.')` assertion. Removed extra `waitForTimeout` after invalid slug submission.

### Files modified
| File | Change |
|------|--------|
| `app/admin/(dashboard)/services/ServiceForm.tsx` | Unified slug input (always editable, controlled by fieldset) |
| `app/actions/admin/services.ts` | Added slug read/validate/unique-check to `updateService()` |
| `tests/unit/actions/services.test.ts` | Added `ensureUniqueSlug` collision handling test |
| `tests/e2e/admin-service-slug.spec.ts` | Replaced `waitForTimeout` with proper toast assertion |

---

## Client-Side Video Compression with ffmpeg.wasm

### New: video compression utility
- `lib/admin/video-compress.ts` (new): Client-side video compression using ffmpeg.wasm single-threaded mode. Loads `@ffmpeg/core` WASM binary (~31 MB) from jsdelivr CDN at runtime (UMD build avoids COOP/COEP headers). Singleton FFmpeg instance with `loadingPromise` concurrency guard — clears on failure to allow retries. FFmpeg args: `-c:v libx264 -preset fast -crf 18 -c:a aac -b:a 192k -movflags +faststart -pix_fmt yuv420p`. Progress tracked by parsing `Duration:` and `time=` from log events. Smart skip: files > 500 MB skip compression (OOM guard); if output >= input, original is returned. WASM filesystem cleanup in `finally` block with unique timestamped filenames. Exports `compressVideo()`, `formatBytes()`, `parseTimeToSeconds()`, `getExtension()`.

### VideoUrlInput — compression step added
- `components/admin/VideoUrlInput.tsx`: Added compression step between file selection and upload. New state: `compressing`, `compressionProgress`, `compressionInfo`. Flow: file → `compressVideo()` → `uploadVideoDirect()`. Progress bar (gold, 4px) during compression. Size comparison text (e.g., "120 MB → 45 MB (62% smaller)") after compression. `mountedRef` guards all `setState` calls after async boundaries to prevent updates on unmounted component. Error message uses `t.upload.failed` (i18n) instead of hardcoded English.

### Upload constants
- `lib/admin/upload-constants.ts`: Added `MAX_COMPRESSIBLE_VIDEO_SIZE = 500 * 1024 * 1024` (500 MB).

### Translation strings
- `messages/admin/en.ts`: Added `compressing` (with `{percent}` placeholder), `compressionDone`, `compressionSkipped`, `loadingFfmpeg` to `upload` section.
- `messages/admin/zh.ts`: Added matching Chinese translations.

### Dependencies
- `package.json`: Added `@ffmpeg/ffmpeg@0.12.15`, `@ffmpeg/util@0.12.2`.

### Unit tests
- `tests/unit/lib/admin/video-compress.test.ts` (new): 19 tests covering `parseTimeToSeconds` (7 tests — standard format, zero, hours, invalid, empty, fractional), `formatBytes` (6 tests — KB, MB boundary, large, fractional, zero), `getExtension` (6 tests — standard, multiple dots, no extension, empty, dotfiles).

### Code review fixes (3 rounds)
**Round 1 — 2 critical, 4 warnings, 4 suggestions:**
- (C1) Added `loadingPromise` guard to `getFFmpeg()` preventing concurrent loads
- (C2) Moved `deleteFile` calls into `finally` block for cleanup on `exec()` failure
- (W1) Added `mountedRef` unmount guard to `VideoUrlInput`
- (W2) Added doc comment about CORE_VERSION compatibility with installed wrapper
- (W3) Replaced `new Uint8Array(data)` copy with `data.buffer.slice()` cast to `ArrayBuffer`
- (W4) Replaced hardcoded `'Upload failed. Please try again.'` with `t.upload.failed`
- (S1) Changed `compressing` translation to use `{percent}` placeholder instead of concatenation
- (S2) Used unique timestamped WASM filenames (`input_{uid}`, `output_{uid}`)
- (S3) Added dev-only `console.warn` in cleanup `.catch()` handlers
- (S4) Added 19 unit tests for pure helper functions

**Round 2 — 1 warning:**
- (W1) `loadingPromise` not cleared on failure — moved `loadingPromise = null` into `finally` block so CDN/load failures allow retries

### E2E tests
- `tests/e2e/admin-video-compress.spec.ts` (new): 4 Playwright tests for video compression on `/admin/company`. Uses programmatically generated minimal MP4 (~1 KB, hand-crafted ftyp+moov+mdat hex). Serial mode with shared admin login. Tests: upload area visibility in edit mode, compression flow triggered on file upload (bilingual status text matching), manual URL input, disabled state in read-only mode. Cleanup removes temp MP4 in `afterAll`.

### Files modified
| File | Change |
|------|--------|
| `lib/admin/video-compress.ts` | New — ffmpeg.wasm compression utility |
| `components/admin/VideoUrlInput.tsx` | Added compression step, progress UI, unmount guard |
| `lib/admin/upload-constants.ts` | Added `MAX_COMPRESSIBLE_VIDEO_SIZE` |
| `messages/admin/en.ts` | Added 4 compression translation keys |
| `messages/admin/zh.ts` | Added 4 compression translation keys |
| `package.json` | Added `@ffmpeg/ffmpeg`, `@ffmpeg/util` |
| `tests/unit/lib/admin/video-compress.test.ts` | New — 19 tests for pure helpers |
| `tests/e2e/admin-video-compress.spec.ts` | New — 4 E2E tests for video compression UI |
| `docs/admin-components.md` | Added Video Compression section, updated VideoUrlInput description |

---

## Video Upload for Projects, Sites & Before/After Pairs

## Database Schema Changes

### New columns added (`lib/db/schema.ts`)
- `project_sites.hero_video_url` (VARCHAR 500) — site hero video
- `projects.hero_video_url` (VARCHAR 500) — project hero video
- `site_image_pairs.before_video_url` / `after_video_url` (VARCHAR 500 each)
- `project_image_pairs.before_video_url` / `after_video_url` (VARCHAR 500 each)

### Updated CHECK constraints
- `project_image_pairs`: `at_least_one_image` → `at_least_one_media` — allows pairs with image(s), video(s), or both
- `site_image_pairs`: `site_at_least_one_image` → `site_at_least_one_media` — same change

## Type System (`lib/types.ts`)

### `ImagePair` interface — added:
- `beforeVideo?: string` — before video URL
- `afterVideo?: string` — after video URL

### `LocalizedImagePair` interface — added:
- `beforeVideo?: string`, `afterVideo?: string`

### `Project` / `LocalizedProject` interfaces — added:
- `hero_video?: string`

### `Site` / `LocalizedSite` interfaces — added:
- `hero_video?: string`

## Query Layer (`lib/db/queries.ts`)

### `DbImagePairBase` interface — added:
- `beforeVideoUrl`, `afterVideoUrl` columns

### `mapDbImagePairRowToImagePair()` — extended:
- Maps `beforeVideoUrl` → `pair.beforeVideo` (via `getAssetUrl()`)
- Maps `afterVideoUrl` → `pair.afterVideo` (via `getAssetUrl()`)

### `mapDbProjectToProject()` — extended:
- Maps `heroVideoUrl` → `project.hero_video` (via `getAssetUrl()`)

### `mapDbSiteToSite()` — extended:
- Maps `heroVideoUrl` → `site.hero_video` (via `getAssetUrl()`)

## Data Localization (`lib/data/projects.ts`)

### `getLocalizedProject()` — passes through:
- `hero_video` from project
- `beforeVideo` / `afterVideo` per image pair

### `getLocalizedSite()` — passes through:
- `hero_video` from site
- `beforeVideo` / `afterVideo` per image pair

## Admin Form Utilities (`lib/admin/form-utils.ts`)

### New shared validation functions:
- `validatePairUrls(pairData)` — validates all image and video URLs in parsed pairs
- `validateExternalProductUrls(epData)` — validates external product URLs

### Updated interfaces:
- `DbImagePairRow`: added `beforeVideoUrl`, `afterVideoUrl`
- `FormImagePair`: added `beforeVideoUrl`, `afterVideoUrl`
- `ParsedImagePair`: added `beforeVideoUrl`, `afterVideoUrl`

### Updated functions:
- `mapDbImagePairToForm()`: maps video URL fields
- `parseImagePairs()`: parses `beforeVideoUrl`/`afterVideoUrl` from FormData; skip condition updated to require at least one image OR one video

## Admin Forms

### `ImagePairEditor.tsx` — video upload per pair side:
- `ImagePairEntry` interface: added `beforeVideoUrl`, `afterVideoUrl`
- Added `VideoUrlInput` (compact mode) below each image zone
- Hidden form inputs emit video URLs for FormData submission
- New pair defaults include empty video URL strings

### `ProjectForm.tsx` — hero video:
- Added `VideoUrlInput` for `heroVideoUrl` below hero image input
- `initialData` interface includes optional `heroVideoUrl`

### `SiteForm.tsx` — hero video:
- Added `VideoUrlInput` for `heroVideoUrl` below hero image input
- `initialData` interface includes optional `heroVideoUrl`

### `VideoUrlInput.tsx` — new props:
- `onChange?: (url: string) => void` — controlled mode callback
- `compact?: boolean` — smaller UI for embedding in ImagePairEditor
- Label styling, spacing, and preview size adapt to compact mode

## Server Actions

### `app/actions/admin/projects.ts`:
- `getProjectData()`: extracts `heroVideoUrl` from FormData
- `createProject()` / `updateProject()`: validates `heroVideoUrl` as valid URL
- Validation refactored: replaced inline pair/product URL checks with shared `validatePairUrls()` and `validateExternalProductUrls()`

### `app/actions/admin/sites.ts`:
- Same changes as projects: `heroVideoUrl` extraction, validation, shared validators

## Front-End Display

### `ProjectDetailPage.tsx`:
- Hero video: renders `<video controls>` above gallery when `hero_video` exists (with `hero_image` as poster)
- Gallery: supports `displayVideo` — renders `<video>` instead of `<Image>` when current pair side has video
- Thumbnails: 3-branch rendering (split before/after images → single image → video-only with `Video` icon placeholder)
- Fullscreen: same 3-branch thumbnail pattern; `poster` attribute on all `<video>` elements; fullscreen button shows when video OR image exists

### `SiteDetailPage.tsx`:
- Same changes as ProjectDetailPage for hero video, gallery video, thumbnails, and fullscreen

### `ProjectModal.tsx`:
- Same changes: `displayVideo` state, video rendering in gallery, 3-branch thumbnails with `Video` icon fallback, `poster` attribute

## Translation Keys

### `messages/admin/en.ts` / `zh.ts`:
- `projects.heroVideoUrl` / `sites.heroVideoUrl`
- `imagePairs.beforeVideoUrl` / `imagePairs.afterVideoUrl`

## Code Review Fixes Applied

1. **Extracted duplicated validation helpers** — `validatePairUrls()` and `validateExternalProductUrls()` moved from inline in both projects.ts and sites.ts to shared `form-utils.ts`
2. **Fixed thumbnail crash on video-only pairs** — replaced non-null assertion `(pair.afterImage || pair.beforeImage)!.src` with 3-branch rendering pattern
3. **Added poster attribute** — all `<video>` elements use `poster={displayImage?.src}` for preview frame
4. **Fixed fullscreen button** — condition changed from `displayImage &&` to `(displayImage || displayVideo) &&`
5. **Rendered hero video on detail pages** — `<video controls playsInline>` with aspect-video above gallery
6. **Used VideoUrlInput in ImagePairEditor** — replaced plain `<input>` with `VideoUrlInput` in compact mode for proper upload UX
7. **Migration verified** — existing `0000_wakeful_clea.sql` already includes all video columns; `pnpm db:generate` confirms no drift
8. **Typecheck passed** — `pnpm typecheck` clean, zero errors

## Files Modified (18 total)

| File | Change |
|------|--------|
| `lib/db/schema.ts` | 6 new columns, 2 updated CHECK constraints |
| `lib/types.ts` | Video fields on ImagePair, LocalizedImagePair, Project, Site, LocalizedProject, LocalizedSite |
| `lib/db/queries.ts` | Video mapping in pair/project/site query functions |
| `lib/data/projects.ts` | Video pass-through in localization helpers |
| `lib/admin/form-utils.ts` | Video fields in interfaces, parsing, shared validators |
| `components/admin/ImagePairEditor.tsx` | VideoUrlInput per pair side, hidden inputs, defaults |
| `components/admin/ProjectForm.tsx` | Hero video input |
| `components/admin/SiteForm.tsx` | Hero video input |
| `components/admin/VideoUrlInput.tsx` | `onChange`, `compact` props |
| `app/actions/admin/projects.ts` | Extract/validate heroVideoUrl, shared validators |
| `app/actions/admin/sites.ts` | Same as projects |
| `app/admin/(dashboard)/sites/[id]/SiteDetailClient.tsx` | Pass heroVideoUrl to SiteForm |
| `app/admin/(dashboard)/sites/[id]/page.tsx` | Select heroVideoUrl in query |
| `components/pages/ProjectDetailPage.tsx` | Hero video, gallery video, thumbnail fixes |
| `components/pages/SiteDetailPage.tsx` | Same as ProjectDetailPage |
| `components/ProjectModal.tsx` | Gallery video, thumbnail fixes |
| `messages/admin/en.ts` | 4 new translation keys |
| `messages/admin/zh.ts` | 4 new translation keys |
