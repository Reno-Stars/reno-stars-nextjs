# 2026-03-20

## Session 2: Process Page — Export Centering Fix, Layout Improvements, Code Review Cleanup

Fixed poster PNG/PDF export centering issue, improved step card layout and reveal timing, extracted SVG illustrations to separate file, and applied two rounds of code review fixes. Files changed: `components/pages/ProcessPage.tsx`, `components/illustrations/process.tsx` (new), `messages/en.json`, `messages/zh.json`.

### Changes

#### 1. Export centering fix — width-locking for html-to-image
- **File:** `components/pages/ProcessPage.tsx`
- Root cause: `html-to-image` renders in SVG `foreignObject` where block elements don't inherit full parent width, breaking `margin: auto` centering
- Fix: Before `toPng` capture, lock all container elements' widths to computed pixel values via `querySelectorAll('section, [class*="max-w-"], [class*="flex-col"], [class*="flex-row"]')`
- After capture, restore original widths in `finally` block
- Cards container changed from `space-y-*` to `flex flex-col items-center gap-*` with `w-full` children

#### 2. Viewport-based card reveal (replaced road Y-position thresholds)
- **File:** `components/pages/ProcessPage.tsx`
- Cards previously appeared based on abstract SVG road Y-positions (`STEP_ROAD_Y_POSITIONS`), causing early steps to pop up before users could see them
- Now uses each card's actual `getBoundingClientRect().top` compared to `windowH * CARD_REVEAL_VIEWPORT` (0.85)
- Removed `STEP_REVEAL_OFFSET`, `STEP_ROAD_Y_POSITIONS`, and `STEP_CIRCLES` constants

#### 3. Step number circles — HTML elements replacing SVG
- **File:** `components/pages/ProcessPage.tsx`
- Replaced SVG-based step number circles (fixed viewBox coordinates) with HTML `<div>` elements positioned at bottom-center of each card row
- SVG circles couldn't align with HTML flow layout; HTML elements use `absolute -bottom-5 left-1/2 -translate-x-1/2`

#### 4. Alternating zigzag card layout
- **File:** `components/pages/ProcessPage.tsx`
- Cards alternate `lg:flex-row` / `lg:flex-row-reverse` for visual dynamism
- Illustration column: `lg:w-[22%]` with directional alignment (`lg:justify-end` / `lg:justify-start`)
- Content column: `lg:w-[78%]`

#### 5. SVG illustrations extracted to separate file
- **New file:** `components/illustrations/process.tsx`
- Extracted 5 step illustrations (`PhoneIllustration`, `MeasureIllustration`, `ContractIllustration`, `ConstructionIllustration`, `KeyIllustration`) and 2 decorative components (`TreeSvg`, `HouseSvg`)
- Also exports `stepIllustrations` array
- Reduced ProcessPage from ~1009 to ~807 lines

#### 6. Discriminated union for trust badges and footer stats
- **File:** `components/pages/ProcessPage.tsx`
- Both `trustBadges` and `footerStats` arrays now use `type: 'stat' | 'rating'` discriminated union instead of checking `badge.rating` boolean
- Type narrowing is now explicit and TypeScript-enforced

#### 7. Stable React keys for lists
- **File:** `components/pages/ProcessPage.tsx`
- Points list: changed from array index `key={i}` to `key={point}` (string content)
- Tags list: changed from array index `key={i}` to `key={tag.icon}` (unique per step)

#### 8. Step label i18n
- **Files:** `components/pages/ProcessPage.tsx`, `messages/en.json`, `messages/zh.json`
- Hardcoded English `STEP {n}` replaced with translation key `process.hero.stepLabel`
- EN: `"STEP {stepNum}"`, ZH: `"第{stepNum}步"`
- Uses `t('process.hero.stepLabel', { stepNum })` for both locales

#### 9. Removed unused `StepData` fields
- **File:** `components/pages/ProcessPage.tsx`
- Removed `pointsKey` and `tagsKey` from `StepData` interface and `steps` array — these were defined but never read (points/tags accessed via loop index)

#### 10. Removed dead CSS
- **File:** `components/pages/ProcessPage.tsx`
- Removed unused `heroFadeIn` keyframe, `heroShimmer` keyframe, and `hero-shimmer-badge` class from inline `<style>` block

#### 11. Derived array lengths from constant
- **File:** `components/pages/ProcessPage.tsx`
- `stepCardRefs` and `visibleSteps` now use `Array(STEP_COUNT).fill(...)` instead of hardcoded 5-element arrays
- `STEP_COUNT = 5` constant added

---

## Session 1: Process Page Hero Redesign + Scroll Road Animation + Poster Export Fixes

Redesigned the `/process/` page hero section and added scroll-driven road animation with step card reveals. Fixed poster PNG export issues. Two files changed: `components/pages/ProcessPage.tsx` (299 insertions, 104 deletions) and `lib/theme.ts` (2 additions).

### Changes

#### 1. Hero section redesign — warm beige background + full-color logo
- **File:** `components/pages/ProcessPage.tsx`
- Replaced dark navy gradient with warm beige gradient (`SURFACE_LIGHT` → `SURFACE` → `SURFACE_DARK`)
- Removed `brightness-0 invert` from logo (was making it colorless white)
- Added `drop-shadow-lg` to logo for depth
- Text changed from white to navy (`TEXT`, `TEXT_MID`) for contrast on light background
- Decorative SVG elements: radial gold glow, grid pattern, diagonal gold accent lines, gold stars
- Trust badges with frosted glass appearance (`bg-white/60 backdrop-blur-sm`)
- Star divider between title and subtitle

#### 2. CSS keyframe entrance animations
- **File:** `components/pages/ProcessPage.tsx`
- `heroFadeUp`: Staggered fade-up entrance for hero elements (title, subtitle, badges)
- `heroGlow`: Pulsing gold glow on radial gradient
- `heroFloat`: Floating motion on decorative stars
- `heroStar`: Rotation + scale animation on star divider
- `heroDividerGrow`: Width expansion for divider line
- Elements tagged with `data-hero-animated="entrance"` or `data-hero-animated="decorative"` for CSS targeting during poster capture

#### 3. Scroll-driven road SVG animation
- **File:** `components/pages/ProcessPage.tsx`
- Road SVG progressively draws using `strokeDasharray`/`strokeDashoffset` based on scroll position
- Dashed center line revealed via SVG `<clipPath>` with growing `<rect>` (preserves dash pattern)
- Step number circles animate with spring bounce (`scale(0)` → `scale(1)`)
- Step cards fade in with `translateY(40px) scale(0.95)` → normal when card enters viewport
- Tagline fades in after last step becomes visible

#### 4. Scroll handler with rAF throttling
- **File:** `components/pages/ProcessPage.tsx`
- Scroll handler wrapped in `requestAnimationFrame` with `cancelAnimationFrame` deduplication
- `setVisibleSteps` only triggers re-render when values actually change (referential equality check)
- Cleanup cancels pending rAF on unmount

#### 5. Module-level constants extracted
- **File:** `components/pages/ProcessPage.tsx`
- `ROAD_VIEWBOX_HEIGHT`, `ROAD_VIEWBOX_WIDTH`, `SCROLL_LEAD`, `CARD_REVEAL_VIEWPORT` — scroll animation tuning
- `ROAD_PATH_D` — shared SVG bezier curve path (used by 3 `<path>` elements)
- `STEP_COUNT` — number of process steps (used by refs and state arrays)

#### 6. Poster PNG export fixes
- **File:** `components/pages/ProcessPage.tsx`
- Added `isCapturing` state to force all animated elements visible during capture
- Uses `flushSync` from `react-dom` for synchronous state updates before `toPng`
- Forces road SVG fully drawn (`strokeDashoffset: '0'`, clipRect height maxed)
- Width-locking pattern for foreignObject centering (locks computed widths before capture)
- CSS overrides target `data-hero-animated` attributes (not coupled to specific class names)
- Restores scroll-driven state after capture via `window.dispatchEvent(new Event('scroll'))`

#### 7. Step card alternating layout
- **File:** `components/pages/ProcessPage.tsx`
- Cards alternate `lg:flex-row` / `lg:flex-row-reverse` for zigzag visual
- Illustration column: `lg:w-[22%] shrink-0`, content column: `lg:w-[78%]`
- Flexbox centering container with `items-center` for export compatibility

#### 8. New theme tokens
- **File:** `lib/theme.ts`
- Added `SURFACE_LIGHT = '#f5f0e8'` — lighter surface variant for gradients
- Added `SURFACE_DARK = '#e0d6c8'` — darker surface variant for gradients
