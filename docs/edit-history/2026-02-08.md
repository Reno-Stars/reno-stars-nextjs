# Edit History: 2026-02-08

## Summary

This session focused on:
1. **Gallery CRUD** — Added create (add new) and delete functionality to the Gallery admin panel, plus a visual Tetris preview grid
2. **Service Areas CRUD** — Added create (add new) and delete functionality to the Service Areas admin panel, with FK safety check for contact submissions
3. **DataTable Enhancement** — Added `headerAction` prop for rendering controls alongside the search bar
4. **Upload Improvements** — Added `S3_PUBLIC_URL` env var support and relative path validation
5. **Form Utils** — `isValidUrl()` now accepts relative paths starting with `/`
6. **Gallery Edit Delete Button** — Added `GalleryEditClient` wrapper with delete functionality and confirmation dialog
7. **ConfirmDialog Centering Fix** — Fixed modal positioning to center on screen
8. **Accessibility Improvements** — Added keyboard focus styles to ConfirmDialog cancel button
9. **useDragReorder Hook** — Extracted reusable drag-and-drop reordering logic with proper cleanup (mountedRef pattern)
10. **DragHandleIcon Component** — Extracted reusable 6-dot drag handle SVG icon
11. **Database Transactions** — Gallery reorder now uses Drizzle transactions for atomicity
12. **Admin Panel Mobile Optimization** — Full responsive mobile support via CSS overrides + DashboardShell component with sidebar drawer, hamburger menu, Escape key, body scroll lock, and single-column form layouts

---

## Gallery — Create & Delete

Added full create and delete CRUD to the Gallery admin panel, matching the established FAQs pattern.

### Server Actions (`app/actions/admin/gallery.ts`)

**`createGalleryItem()`** — New action:
- Validates image URL (required, valid format), category, title lengths, display order
- Inserts via `db.insert(galleryItems).values(...)`
- Redirects to `/admin/gallery` after creation (uses `redirect()` outside try/catch)

**`deleteGalleryItem()`** — New action:
- Validates UUID, deletes with `.returning()` check
- No FK constraints to check (gallery items are standalone)

### Form (`GalleryItemForm.tsx`)

Added `isNew` prop support:
- `initialData` now optional, with sensible defaults (kitchen category, published by default)
- `EditModeToggle` hidden when `isNew`
- Submit button label switches between create/update
- `useEffect` for category sync guards against undefined `initialData`

### List Client (`GalleryListClient.tsx`)

Major enhancements:
- **Delete button + ConfirmDialog** — Matching FAQs pattern
- **Tetris Preview Grid** — Visual preview of published gallery items using the same layout pattern as the frontend `TetrisGallery.tsx` (12-item repeating pattern with `col-span-2` for wide items)
- **Category Filter Buttons** — Filter the preview grid by gallery category (All / Kitchen / Bathroom / Whole House / Commercial)

### New Page (`app/admin/(dashboard)/gallery/new/page.tsx`)

Standard create page with `AdminPageHeader` + `GalleryItemForm` with `isNew` prop.

### Translations

| Key | EN | ZH |
|-----|----|----|
| `gallery.addGalleryItem` | `'Add Gallery Item'` | `'添加图片'` |
| `gallery.createGalleryItem` | `'Create Gallery Item'` | `'创建图片'` |

---

## Service Areas — Create & Delete

Added full create and delete CRUD to the Service Areas admin panel with FK safety.

### Server Actions (`app/actions/admin/service-areas.ts`)

**`createServiceArea()`** — New action:
- Validates slug (required, `isValidSlug()`), names (required), descriptions, display order
- Uses `ensureUniqueSlug()` for collision safety (queries existing slugs first)
- Inserts via `db.insert(serviceAreas).values(...)`

**`deleteServiceArea()`** — New action:
- **FK Safety Check**: Queries `contact_submissions` table for rows referencing this area via `preferredAreaId`
- If any contacts reference the area, returns error: `"Cannot delete: X contact(s) reference this area."`
- Only deletes if no FK references exist

### Form (`ServiceAreaForm.tsx`)

Added `isNew` prop support:
- `initialData` now optional, with sensible defaults
- Slug field: editable `<input name="slug">` in create mode, read-only display-only in edit mode
- `EditModeToggle` hidden when `isNew`
- Submit button label: `createServiceArea` vs `update`

### List Client (`ServiceAreasListClient.tsx`)

- Delete button + `ConfirmDialog` matching FAQs pattern
- Shared `useTransition` between toggle and delete (matches existing pattern)

### List Page (`page.tsx`)

Added `actionKey` and `actionHref` to `AdminPageHeader` for the "Add Service Area" button.

### New Page (`app/admin/(dashboard)/service-areas/new/page.tsx`)

Standard create page with `AdminPageHeader` + `ServiceAreaForm` with `isNew` prop.

### Translations

| Key | EN | ZH |
|-----|----|----|
| `serviceAreas.addServiceArea` | `'Add Service Area'` | `'添加服务区域'` |
| `serviceAreas.addNewServiceArea` | `'Add New Service Area'` | `'新建服务区域'` |
| `serviceAreas.createServiceArea` | `'Create Service Area'` | `'创建服务区域'` |

---

## DataTable Enhancement

### `headerAction` Prop (`components/admin/DataTable.tsx`)

New optional `headerAction: ReactNode` prop renders content on the right side of the search bar row. The search bar and header action are wrapped in a flexbox with `justify-content: space-between`.

Used by the Gallery list client for category filter buttons.

---

## Upload Improvements (`app/actions/admin/upload.ts`)

- **`S3_PUBLIC_URL` env var**: New environment variable for the public-facing URL of the S3 bucket (e.g., R2 public bucket URL or MinIO public URL)
- Falls back to `NEXT_PUBLIC_STORAGE_PROVIDER` for backwards compatibility
- Now returns an error if neither `S3_PUBLIC_URL` nor `NEXT_PUBLIC_STORAGE_PROVIDER` is set (previously would return a relative path like `/gallery/image.jpg` which isn't valid)

---

## Form Utils (`lib/admin/form-utils.ts`)

`isValidUrl()` now accepts relative paths starting with `/` in addition to `http://` and `https://` URLs. This supports local/relative image paths.

---

## New Files

| File | Description |
|------|-------------|
| `app/admin/(dashboard)/gallery/new/page.tsx` | Gallery item create page |
| `app/admin/(dashboard)/service-areas/new/page.tsx` | Service area create page |

## Modified Files

| File | Changes |
|------|---------|
| `app/actions/admin/gallery.ts` | Added `createGalleryItem()` + `deleteGalleryItem()` |
| `app/actions/admin/service-areas.ts` | Added `createServiceArea()` + `deleteServiceArea()` |
| `app/actions/admin/upload.ts` | `S3_PUBLIC_URL` env var support, error on missing config |
| `app/admin/(dashboard)/gallery/GalleryItemForm.tsx` | `isNew` prop support |
| `app/admin/(dashboard)/gallery/GalleryListClient.tsx` | Delete button, Tetris preview grid, category filter |
| `app/admin/(dashboard)/service-areas/ServiceAreaForm.tsx` | `isNew` prop support, editable slug |
| `app/admin/(dashboard)/service-areas/ServiceAreasListClient.tsx` | Delete button + ConfirmDialog |
| `app/admin/(dashboard)/service-areas/page.tsx` | "Add Service Area" button |
| `components/admin/DataTable.tsx` | `headerAction` prop |
| `lib/admin/form-utils.ts` | `isValidUrl()` accepts relative paths |
| `messages/admin/en.ts` | 5 new translation keys (gallery + service areas) |
| `messages/admin/zh.ts` | 5 new translation keys (gallery + service areas) |

---

## Gallery Edit Page — Delete Button

Added a delete button to the gallery edit page (`/admin/gallery/[id]`), matching the pattern established in FAQs and other CRUD pages.

### New Component (`GalleryEditClient.tsx`)

Wrapper component that combines `GalleryItemForm` with delete functionality:
- Delete button with `useTransition` for pending state
- `ConfirmDialog` for delete confirmation
- Toast feedback on success/error
- Navigation to `/admin/gallery` after successful delete

### Page Update (`app/admin/(dashboard)/gallery/[id]/page.tsx`)

Changed from rendering `GalleryItemForm` directly to rendering `GalleryEditClient` wrapper, passing the bound `updateGalleryItem` action and initial data.

---

## ConfirmDialog — Centering Fix

The `ConfirmDialog` modal was appearing in the top-left corner instead of centered on screen.

### Fix (`components/admin/ConfirmDialog.tsx`)

Added explicit centering styles to the `<dialog>` element:
```typescript
style={{
  position: 'fixed',
  top: '50%',
  left: '50%',
  transform: 'translate(-50%, -50%)',
  margin: 0,
  // ... other styles
}}
```

### Accessibility Enhancement

Added keyboard focus styles to the cancel button for better accessibility:
```typescript
onFocus={(e) => {
  e.currentTarget.style.outline = `2px solid ${NAVY}`;
  e.currentTarget.style.outlineOffset = '2px';
}}
onBlur={(e) => {
  e.currentTarget.style.outline = 'none';
}}
```

---

## useDragReorder Hook

Extracted drag-and-drop reordering logic into a reusable hook at `hooks/useDragReorder.ts`.

### Features

- Generic type parameter `<T>` for item type
- Configurable callbacks: `getId`, `getDisplayOrder`, `isIncluded`, `onReorder`, `onSuccess`, `onError`
- Optimistic UI updates with server sync
- Proper cleanup with `mountedRef` pattern to prevent state updates after unmount
- Unhandled rejection handling with `.catch()`
- `shouldNavigate()` helper to distinguish clicks from drags

### Usage

```typescript
const {
  draggedId,
  dragOverId,
  localOrder,
  isReordering,
  handleDragStart,
  handleDragOver,
  handleDragLeave,
  handleDrop,
  handleDragEnd,
  shouldNavigate,
} = useDragReorder({
  items,
  getId: (item) => item.id,
  getDisplayOrder: (item) => item.displayOrder,
  isIncluded: (item) => item.isPublished,
  onReorder: reorderGalleryItems,
  onSuccess: () => router.refresh(),
  onError: (msg) => toast(msg, 'error'),
});
```

---

## DragHandleIcon Component

Extracted the 6-dot drag handle SVG into a reusable component at `components/admin/DragHandleIcon.tsx`.

### Props

```typescript
export interface DragHandleIconProps {
  size?: number;    // Default: 20
  className?: string;
}
```

### Implementation

Uses an SVG with 6 circles arranged in a 2x3 grid pattern, commonly used as a drag affordance.

---

## Gallery Actions — Transactions and Code Review Fixes

### Database Transaction (`reorderGalleryItems`)

Changed from `Promise.all` to `db.transaction()` for atomic updates:

```typescript
await db.transaction(async (tx: typeof db) => {
  for (let i = 0; i < orderedIds.length; i++) {
    await tx.update(galleryItems)
      .set({ displayOrder: i })
      .where(eq(galleryItems.id, orderedIds[i]));
  }
});
```

### Documentation Comments

Added file-level logging note:
```typescript
// NOTE: console.error is used for error logging until a structured logger is available.
```

### Upload Action Documentation

Expanded comment on `S3_PUBLIC_URL` fallback chain in `app/actions/admin/upload.ts`.

---

## New Files

| File | Description |
|------|-------------|
| `app/admin/(dashboard)/gallery/[id]/GalleryEditClient.tsx` | Gallery edit page wrapper with delete button |
| `components/admin/DragHandleIcon.tsx` | Reusable 6-dot drag handle SVG icon |
| `hooks/useDragReorder.ts` | Reusable drag-and-drop reordering hook |
| `tests/unit/actions/gallery.test.ts` | Unit tests for gallery server actions |
| `tests/unit/hooks/useDragReorder.test.ts` | Unit tests for useDragReorder hook |

## Modified Files (Later Session)

| File | Changes |
|------|---------|
| `app/actions/admin/gallery.ts` | Transaction for reorder, logging comment, result variable pattern |
| `app/actions/admin/upload.ts` | Expanded documentation for S3_PUBLIC_URL |
| `app/admin/(dashboard)/gallery/[id]/page.tsx` | Uses `GalleryEditClient` wrapper |
| `components/admin/ConfirmDialog.tsx` | Fixed centering, added focus styles to cancel button |

---

## Admin Panel Mobile Optimization

Added responsive mobile support to the entire admin dashboard. Uses a CSS-only override strategy (`admin-responsive.css`) with `className` attributes alongside existing inline styles — CSS `!important` at `@media (max-width: 768px)` overrides inline values. Zero visual change on desktop.

### New Files

| File | Description |
|------|-------------|
| `app/admin/admin-responsive.css` | Mobile-only CSS overrides (sidebar drawer, form grids, cards, page header, etc.) |
| `components/admin/DashboardShell.tsx` | Client component extracted from dashboard layout for sidebar state + mobile drawer |
| `hooks/useIsMobile.ts` | `matchMedia` hook returning boolean for mobile breakpoint detection |

### Architecture: DashboardShell

The dashboard layout (`app/admin/(dashboard)/layout.tsx`) is a server component (for `requireAuth()`). Client-side sidebar open/close state is handled by `DashboardShell`:
- Sidebar wrapper with `className="admin-sidebar"` + conditional `admin-sidebar--open`
- Overlay div (mobile only, `aria-hidden="true"`)
- Closes sidebar on: overlay click, Escape key, link navigation, resize to desktop
- Uses `useIsMobile()` to auto-close sidebar on desktop transition

### AdminLocaleProvider Changes

Added `sidebarOpen: boolean` + `setSidebarOpen` to the existing context (extends the interface). Context value is now memoized with `useMemo`.

### Sidebar Changes

- Added optional `onNavigate?: () => void` prop, called on each `<Link onClick={onNavigate}>`
- Changed `flexShrink: 0` → `flex: 1` so the nav fills its wrapper's full height (fixes white gap at bottom)

### TopBar Changes

- Added hamburger button (3-line SVG) with `className="admin-hamburger"` (CSS-hidden on desktop)
- Reads `setSidebarOpen` from context
- Translated `aria-label` (`t.topBar.openMenu`)

### CSS Overrides (`admin-responsive.css`)

All rules inside `@media (max-width: 768px)`:

| Class | Override |
|-------|----------|
| `.admin-sidebar` | `position: fixed`, `transform: translateX(-100%)`, `height: 100dvh` (with `100vh` fallback), `overflow-y: auto` |
| `.admin-sidebar--open` | `transform: translateX(0)` |
| `body:has(.admin-sidebar--open)` | `overflow: hidden` (scroll lock) |
| `.admin-sidebar-overlay` | `display: block`, fixed backdrop with `z-index: 999` |
| `.admin-hamburger` | `display: flex` |
| `.admin-topbar` | Reduced padding |
| `.admin-main-content` | Reduced padding |
| `.admin-form-grid` | `grid-template-columns: 1fr` (single column) |
| `.admin-form-card` | `padding: 1rem`, `max-width: 100%` |
| `.admin-page-header` | `flex-direction: column` (stack vertically) |
| `.admin-site-detail-grid` | `grid-template-columns: 1fr` (single column) |

Desktop rules (always applied): `.admin-sidebar-overlay { display: none }`, `.admin-hamburger { display: none }`.

### className Additions (no behavior change on desktop)

Added `className="admin-form-grid"` to 17 grid divs across 11 form files:
- `BilingualInput.tsx`, `BilingualTextarea.tsx`
- `ProjectForm.tsx` (4 grids), `SiteForm.tsx` (1 grid)
- `GalleryItemForm.tsx` (2), `BlogPostForm.tsx` (1), `ServiceAreaForm.tsx` (2), `ShowroomForm.tsx` (1), `TrustBadgeForm.tsx` (2), `SocialLinkForm.tsx` (1), `CompanyForm.tsx` (1)

Added `className="admin-form-card"` to 12 form card containers:
- `ProjectForm.tsx`, `SiteForm.tsx`, `GalleryItemForm.tsx`, `BlogPostForm.tsx`, `ServiceAreaForm.tsx`, `ShowroomForm.tsx`, `TrustBadgeForm.tsx`, `AboutForm.tsx`, `SocialLinkForm.tsx`, `FaqForm.tsx`, `ServiceEditForm.tsx`, `CompanyForm.tsx`

Added `className="admin-page-header"` to `AdminPageHeader.tsx`.
Added `className="admin-site-detail-grid"` to `SiteDetailClient.tsx`.

### Translation Keys

| Key | EN | ZH |
|-----|----|----|
| `topBar.openMenu` | `'Open menu'` | `'打开菜单'` |

### Modified Files

| File | Changes |
|------|---------|
| `app/admin/admin-responsive.css` | **New** — Mobile CSS overrides |
| `components/admin/DashboardShell.tsx` | **New** — Client shell with sidebar drawer |
| `hooks/useIsMobile.ts` | **New** — Mobile breakpoint hook |
| `app/admin/layout.tsx` | Import `admin-responsive.css` |
| `app/admin/(dashboard)/layout.tsx` | Replaced inline layout with `<DashboardShell>` |
| `components/admin/AdminLocaleProvider.tsx` | Added `sidebarOpen`/`setSidebarOpen`, `useMemo` for value |
| `components/admin/Sidebar.tsx` | Added `onNavigate` prop, `flex: 1` |
| `components/admin/TopBar.tsx` | Added hamburger button |
| `components/admin/AdminPageHeader.tsx` | Added `className="admin-page-header"` |
| `messages/admin/en.ts` | Added `topBar.openMenu` |
| `messages/admin/zh.ts` | Added `topBar.openMenu` |
| 12 form files | Added `admin-form-grid` / `admin-form-card` classNames |
| `SiteDetailClient.tsx` | Added `admin-site-detail-grid` className |

---

## Code Review Fixes

Comprehensive code review identified and fixed 13 issues across robustness, accessibility, performance, and test coverage.

### SSR Hydration Fix (`hooks/useIsMobile.ts`)

Fixed potential hydration mismatch by initializing state with a function that checks `window` availability:

```typescript
const [isMobile, setIsMobile] = useState(() => {
  if (typeof window === 'undefined') return false;
  return window.matchMedia(`(max-width: ${breakpoint}px)`).matches;
});
```

### localStorage Error Handling (`components/admin/AdminLocaleProvider.tsx`)

Added try/catch around localStorage access for robustness in private browsing mode:

```typescript
try {
  const stored = localStorage.getItem(STORAGE_KEY);
  // ...
} catch {
  // localStorage may be unavailable in private browsing mode
}
```

### Focus Indicators (`components/admin/ConfirmDialog.tsx`, `app/admin/admin-responsive.css`)

Replaced inline JavaScript focus handlers with CSS `:focus-visible` for better accessibility:

```css
.confirm-dialog-btn:focus-visible {
  outline: 2px solid var(--color-navy, #1B365D);
  outline-offset: 2px;
}
```

Added `--color-navy` CSS custom property in `:root` for theme consistency.

### DashboardShell Effect Optimization (`components/admin/DashboardShell.tsx`)

Fixed effect to only close sidebar when transitioning from mobile to desktop (not on every `isMobile` change):

```typescript
const prevMobileRef = useRef(isMobile);
useEffect(() => {
  if (prevMobileRef.current && !isMobile) {
    setSidebarOpen(false);
  }
  prevMobileRef.current = isMobile;
}, [isMobile, setSidebarOpen]);
```

### Sidebar Path Normalization (`components/admin/Sidebar.tsx`)

Added trailing slash normalization for consistent path matching:

```typescript
const normalizedPath = pathname.replace(/\/$/, '');
const normalizedHref = href.replace(/\/$/, '');
```

### Slug Validation (`lib/admin/form-utils.ts`)

Updated regex to reject consecutive hyphens (e.g., `a--b`):

```typescript
const SLUG_RE = /^[a-z0-9]+(-[a-z0-9]+)*$/;
```

### Named Constants (`hooks/useDragReorder.ts`)

Extracted magic number to named constant with JSDoc:

```typescript
/** Minimum pixel movement to distinguish a drag from a click */
const DRAG_THRESHOLD_PX = 5;
```

### Tailwind Ring Color (`app/admin/(dashboard)/gallery/GalleryListClient.tsx`)

Replaced CSS variable workaround with proper Tailwind class:

```tsx
className="... focus:ring-amber-500"
```

### Unused Import Removal (`lib/db/queries.ts`)

Removed unused `sql` import from drizzle-orm.

### New Test Files

| File | Tests | Coverage |
|------|-------|----------|
| `tests/unit/hooks/useIsMobile.test.ts` | 6 | Mobile detection, cleanup, breakpoint |
| `tests/unit/components/admin/AdminLocaleProvider.test.tsx` | 8 | Context, localStorage, error handling |
| `tests/unit/components/admin/ConfirmDialog.test.tsx` | 10 | Dialog state, a11y, focus |
| `tests/unit/lib/admin/form-utils.test.ts` | +2 | Consecutive hyphen rejection |

### Modified Files

| File | Changes |
|------|---------|
| `hooks/useDragReorder.ts` | `DRAG_THRESHOLD_PX` constant, removed unused generic |
| `hooks/useIsMobile.ts` | SSR-safe lazy initialization |
| `components/admin/AdminLocaleProvider.tsx` | try/catch for localStorage |
| `components/admin/ConfirmDialog.tsx` | CSS class for focus, removed inline handlers |
| `components/admin/DashboardShell.tsx` | `prevMobileRef` for transition detection |
| `components/admin/Sidebar.tsx` | Path normalization |
| `app/admin/admin-responsive.css` | `--color-navy` variable, `.confirm-dialog-btn` focus styles |
| `app/admin/(dashboard)/gallery/GalleryListClient.tsx` | Tailwind `focus:ring-amber-500` |
| `app/actions/admin/gallery.ts` | ESLint disable comment for transaction type |
| `lib/admin/form-utils.ts` | Slug regex update |
| `lib/db/queries.ts` | Removed unused `sql` import |

### Final Status

- **Tests:** 196 passing (11 test files)
- **TypeScript:** No errors
- **ESLint:** No warnings
