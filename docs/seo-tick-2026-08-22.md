# SEO Tick — 2026-08-22

## Branch
`seo/daily-2026-08-22`

## PRs / Branches
- no open PRs (gh unavailable — tooling gap)
- Branch pushed: `seo/daily-2026-08-22` (origin/main baseline, no commits yet)

---

## Findings

### 1. DB Query API — BLOCKED (403)
**Endpoint:** `https://api.reno-stars.com/query`
**Token:** `DB_QUERY_API_TOKEN` env var
**Error:** HTTP 403 on all requests

Verified with:
```
POST https://api.reno-stars.com/query
Authorization: Bearer <token>
{"sql":"SELECT id FROM blog_posts LIMIT 1"}
→ 403 Forbidden
```

Also tried internal K8s endpoint (`http://enteros-tenant.enteros-reno-stars.svc.cluster.local:8080/`) — connection timeout.

**Impact:** Ladder item 1 (content integrity checks) cannot run. All `seo_keywords_zh`, `excerpt_zh`, and other localized-field checks are blocked.

**Status:** This is a network/credential issue in this runtime, NOT a data defect. The DB itself may be healthy. The credential may have been revoked or the API gateway may block external runtime IPs.

---

### 2. Site Availability — DOWN (530 Cloudflare origin unreachable)

**All** endpoints tested now return HTTP 530:
- `https://reno-stars.com/en/` → 530
- `https://reno-stars.com/en/services/` → 530
- `https://reno-stars.com/sitemap.xml` → 530

530 is Cloudflare's "origin DNS failure" — the k3s cluster (ns/reno-stars) is not reachable from this runtime's network path. This is a network/infrastructure issue, not a code defect. The site's last-known-good state (prior tick) showed homepage rendering correctly with valid schema.

Previous tick showed: homepage HTTP 200 + correct JSON-LD, service/blog pages 200 + 404 content (DB cascade). Both issues now masked by 530.

**Pending:** Site recovers from 530 → two findings still present:
1. DB API 403 → service/blog pages still render 404 content
2. Schema/metadata from prior tick: homepage clean, service pages unconfirmed

---

### 3. Migration Backlog (ladder item 1)

No new migrations written — DB is inaccessible, so no new NULL/English-row queries can run.

Pending migrations in `scripts/migrations/`:
- `2026-08-16/`: excerpt_zh batches 1–3 (3 files, excerpt_zh AT CAP per prior tick)
- `2026-08-17/`: excerpt_zh batches 4–7
- `2026-08-21/`: excerpt_zh batch 8
- `excerpt_zh backlog: 3 files pending human apply (batch1+2+3 from 08-16, which superseded 08-17 batches 4–7) — pending human apply`
- `seo_keywords_zh on services.seo_keywords_zh and service_areas.seo_keywords_zh: NOT YET AUDITED`

**Ladder item 1 is blocked on DB restore.**

---

### 4. Schema / Metadata (ladder item 2 — homepage only, static pages)

Homepage `/en/` metadata: **CLEAN**
- Canonical URL: `https://www.reno-stars.com/`
- JSON-LD: Organization + WebSite + 5 Review entities (all valid)
- `og:title`, `og:description`, `og:image` present
- `contactPoint.availableLanguage` = `["English", "Mandarin Chinese", "Cantonese"]` — correct (gated on `nativeSupport`)
- No hreflang links visible in HTML source of homepage (these are likely injected client-side or via Next.js head management not visible in static curl)

Service pages could not be checked (404 from DB failure).

---

### 5. Sitemap
Not checked — prior curl timed out at 15s. Skipping per timeout block.

---

## Blockers

| Blocker | Impact | Resolution |
|---------|--------|------------|
| DB Query API 403 | Cannot run content integrity checks | Restore API access or provide alternative DB read credential |
| Site HTTP 530 (origin unreachable) | All live page verification blocked | k3s cluster network path — not a code issue |

---

## Work Ladder Status

| Item | Status | Reason |
|------|--------|--------|
| 1. Content Integrity | **BLOCKED** | DB API 403 |
| 2. Schema + Metadata | **Partial** | Homepage clean; dynamic pages 404 from DB cascade |
| 3. Coverage Gap | **Not checked** | DB inaccessible |
| 4. Technical | **Not checked** | DB inaccessible |

---

## Next Actions (next tick)

1. **Site recovers from 530** — two issues still present:
   - DB API 403 → ladder item 1 blocked, service/blog pages render 404 content
   - Once 530 clears: verify homepage schema still clean
2. **DB API restore** — most critical; all ladder items depend on it
3. Once DB restored: audit `seo_keywords_zh` on `services` and `service_areas` tables (not yet audited)
4. Check sitemap accessibility

---

*seo/daily-2026-08-22 — tick 2*
