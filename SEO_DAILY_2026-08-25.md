# SEO Audit Notes — 2026-08-25

## /seo-hreflang — Locale URL Health

**结论：✅ 无 hreflang 问题**

### 检查项

| Signal | 结果 |
|--------|------|
| sitemap.xml ×html:link hreflang（14 locales + x-default） | ✅ |
| sitemap x-default → /en/（非裸 `/`） | ✅ |
| HTML <link rel="alternate"> via metadata API（buildAlternates） | ✅ |
| next-intl alternateLinks header | ✅ 主动禁用（与 sitemap 冲突） |
| buildAlternates() BCP-47：zh-Hant 未误映射为 zh-TW | ✅ |
| buildAlternates() x-default 范围 | ✅ /en{path} |
| og:locale:alternate（buildAlternateLocales） | ✅ 13 个其他 locale，未按 nativeSupport 过滤 |
| ogLocale codes：全部含 region 子标签 | ✅ |
| localePrefix always | ✅ |
| service×city hreflang 范围 | ✅ 仅 en+zh（GSC 数据决定） |
| project leaf hreflang 范围 | ✅ 全部 14 locale（2026-08-07 起） |
| Canonical URLs | ✅ buildAlternates canonical 自指 |

### 关键实现细节

- `i18n/config.ts`：`alternateLinks: false` — next-intl HTML header 禁用，因其 x-default 指向裸根 URL `/`（308 重定向到 /en/），与 sitemap + buildAlternates 的 /en/ 冲突
- `lib/utils.ts buildAlternates()`：所有 14 locale 的 hreflang + canonical，正确处理 x-default
- `lib/utils.ts buildAlternateLocales()`：返回所有 13 个其他 ogLocale，未按 nativeSupport 过滤（正确：og:locale:alternate 是内容/翻译元数据）
- `app/sitemap.ts`：service×city 仅 en+zh；project leaf 全部 14 locale

### 0 个问题，0 个 action items
