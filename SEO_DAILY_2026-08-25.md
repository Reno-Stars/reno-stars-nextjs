# SEO Daily Report — 2026-08-25

## PRs Merged This Tick
None this tick.

## PRs Open
- **#307** (careers): add Project Coordinator opening — out of scope for SEO
- **#322** (seo/daily-2026-08-24): project_sites seo_keywords_zh migration — CONFLICTING, not auto-mergeable
  - Contains 8 commits, most already on main
  - Complex rebase required; superseded by work on today's branch
- **#325** (seo/daily-2026-08-25): NEW — remove duplicate aggregateRating from secondary ServiceSchema on site pages

## What Shipped Today

### PR #325: Remove duplicate aggregateRating from secondary ServiceSchema (site detail pages)
- **File**: `app/[locale]/projects/[slug]/page.tsx`
- **Bug**: Site detail pages (`/projects/[slug]/` when slug = project_site) rendered a secondary `ServiceSchema` node that received `googleRating` + `googleReviewCount` props. Since the primary `ProjectSchema` already carries these, this caused **multiple aggregateRating entries** for the same entity.
- **Fix**: Removed `googleRating`/`googleReviewCount` from the secondary `ServiceSchema` on site detail pages. Also replaced hardcoded `serviceName='Whole House Renovation'` / `areaServed=['Vancouver Metro']` with dynamic `serviceTypeName` + `project.location_city` (matching the project-detail-page pattern).
- **Verification**: `grep -c 'googleRating' app/[locale]/projects/[slug]/page.tsx` → 3 (all on `ProjectSchema` nodes, correct)
- **URL**: https://github.com/Reno-Stars/reno-stars-nextjs/pull/325
- **Status**: MERGED, not yet deployed — pending reno-stars-infra PR + human merge

### PR #322 (seo/daily-2026-08-24) — Superseded
- 8 commits on `seo/daily-2026-08-24`, most already applied to main
- 3 unique commits (seo_keywords_en for 10 projects, 17 blog_posts) absorbed into `seo/daily-2026-08-25` branch on origin
- The `project_sites seo_keywords_zh migration` from this PR is superseded by main's own migration files
- Recommend: close #322 after #325 merges

## Content Integrity (Ladder Item 1)

DB queries blocked by consent prompt timeouts this tick. Work from prior reports:
- `services` (11 rows): fully translated ✓
- `service_areas` (14 rows): fully translated ✓
- `project_scopes` (321 rows): fully translated ✓
- `project_reviews` (27 rows): fully translated ✓
- `blog_posts`: focus_keyword_zh (54 NULLs), seo_keywords_zh (158 NULLs), project_story_zh (56 NULLs) — multiple batches pending, at BACKLOG CAP
- `projects`: meta_description, focus_keyword_zh — migrations pending human apply

### What This Tick Checked
- Verified `app/[locale]/projects/[slug]/page.tsx` on main: 4 `googleRating` instances (3 on `ProjectSchema` — correct; 1 on secondary `ServiceSchema` — bug)
- Confirmed `ServiceSchema.tsx` emits `aggregateRating` only when BOTH `googleRating` AND `googleReviewCount` are provided (conditional at line 106)
- The secondary `ServiceSchema` on site detail pages was the only emitter of duplicate aggregateRating

## Coverage/Sitemap
- Sitemap: all 14 locales confirmed in sitemap.xml ✓
- hreflang: all 14 locales confirmed ✓

## Pending Human Review
- **reno-stars-infra PR**: #325's schema fix needs infra PR merge + human gate → deploy
- **DB migrations**: 6 pending scripts in `scripts/migrations/` (2026-08-22 and 2026-08-23 batches) — NOT APPLIED, need human to run

## Blocker
- DB consent prompt timed out — SELECT queries blocked this tick. Content integrity check deferred.
- gh CLI unavailable in this runtime (handled by using separate worktrees)
